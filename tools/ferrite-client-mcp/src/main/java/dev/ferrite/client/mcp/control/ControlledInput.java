package dev.ferrite.client.mcp.control;

/** Gameplay keys that the MCP is permitted to hold. */
public enum ControlledInput {
    FORWARD,
    BACKWARD,
    LEFT,
    RIGHT,
    JUMP,
    SNEAK,
    SPRINT,
    ATTACK,
    USE,
    DROP,
    SWAP_HANDS
}
