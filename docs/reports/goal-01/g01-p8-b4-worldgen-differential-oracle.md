# G01-P8-B4 — Official world-generation differential oracle

## Result

Complete. Goal 01 now has a machine-bound semantic denominator, an official Minecraft 26.2 Anvil
normalizer, a Ferrite snapshot normalizer, and a stage-ordered first-divergence comparator. The
oracle was exercised against a real world generated by the locked official server.

This result deliberately does not claim vanilla-identical generation. The smoke comparison exposed
a real current mismatch, proving the oracle can reject Ferrite output and establishing concrete
input for `G01-P8-B5`.

## Frozen denominator and owners

- Contract: [`worldgen-exactness.toml`](../../../goals/minecraft-java-26.2/worldgen-exactness.toml)
  at SHA-256 `a63d04184be0fa73cebe8a2ef715b0932b44f3d055955f95ea83de4b97a510e6`.
- Implementation: `crates/ferrite-testkit/src/worldgen_oracle/`.
- CLI: `apps/behavior-runner/src/main.rs`.
- Acceptance owner: `apps/behavior-runner/tests/worldgen_differential_oracle.rs`.
- Machine progress: schema-version-2 `worldgen_exactness` record in
  `goals/minecraft-java-26.2/implementation.toml`.

The contract enumerates 16 semantic field families, all three dimensions, two data-pack
populations, seven vanilla-core seeds, positive/negative/boundary/far chunks, and five request
plans. The B4 record is `Implemented`; `Verified` is reserved for the B5 population result.

## Real official-server evidence

The locked `target/mc-reference/26.2/server.jar` ran under Azul JDK 25 with vanilla packs, seed
`2602`, structures enabled, normal Overworld generation, view/simulation distance 2, offline mode,
and port 0. The console forced and saved chunk `0,0`, issued `save-all flush`, and stopped cleanly.
The official log recorded Minecraft `26.2`, readiness, and a successful save. The resulting world
confirmed the 26.2 Overworld path `dimensions/minecraft/overworld/region`.

Normalization produced:

```text
official semantic digest  02149e76a203fd816555c7fcef14afc78567228cca21268046468a8fd6c4ff0b
Ferrite semantic digest   21822da20d73231c758b0fdd101019629223944294649cf25e2dbeff68641575
```

The comparator rejected the pair with the first structured divergence:

```json
{
  "stage": "noise_or_later",
  "field": "block_states",
  "coordinate": [0, -64, 0],
  "official": "\"minecraft:bedrock\"",
  "ferrite": "\"minecraft:stone\""
}
```

This is expected B5 backlog, not an oracle failure. All raw jars, world data, logs, and normalized
documents stayed under ignored `target/` storage.

## Verification

Focused verification passed:

```text
cargo test -p ferrite-testkit --lib worldgen_oracle
cargo test -p behavior-runner --test worldgen_differential_oracle
cargo test -p mc-reference --lib
cargo test -p ferrite-tooling
cargo run -q -p mc-reference --bin mc-ref -- implementation-manifest verify
cargo run -q -p mc-reference --bin mc-ref -- verify --offline
```

The full batch also ran the workspace format, Clippy, test, readiness, diff, source-policy, and
repository-hygiene gates before commit. The terminal audit passed architecture, source, public API,
generated-artifact, and manifest verification before stopping at the expected B5 boundary:
`worldgen_exactness` is `Implemented`, not yet `Verified`. Repository hygiene now permits only the
exact reviewed Gradle Wrapper path and digest needed by the client MCP project; ignored
Gradle/cache/runtime roots
are bounded explicitly and no Mojang jar or generated world is permitted in tracked source.

## Remaining work

`G01-P8-B5` must implement exact official 26.2 generation and prove all declared population cases.
The current Ferrite oracle helper exposes only the Overworld generator and that generator already
diverges at the bedrock layer. Nether/End adapters, official population orchestration, supported
data-pack injection, request-plan execution, and elimination of every reported semantic difference
remain required. Goal 01 therefore remains `InProgress` after this batch.
