# Goal 03 Launch Prompt

Use this prompt to start or resume persistent execution of
[Goal 03](03-production-runtime-integration.md).

```text
Create and start a persistent goal with this objective:

Complete Ferrite Goal 03: establish the production-integration manifest, remove Goal 01 phase
numbers from active architecture through safe compatibility migrations, compose the formal
Minecraft Region runtime, make protocol dispatch and projection explicit, and prove the path with
the exact 26.2 client MCP. Continue batch by batch until every terminal gate has committed evidence.
Do not set a token budget.

Before changing code, read AGENTS.md, the Goal 03 plan and status ledger, docs/architecture.md,
Goal 01 supported contracts and completion evidence, Goal 02 client MCP operations and completion
record, and every prior Goal 03 decision. Inspect Git status and preserve user work.

Select the earliest unblocked pending batch and keep exactly one batch InProgress. Implement one
responsibility-bounded batch with focused tests and documentation. Do not rename historical report
paths or genuine domain state-machine phases. Treat persisted phase-numbered resource identities as
versioned compatibility surfaces: migrations must be bounded, idempotent, crash-safe, inspectable,
and fail closed. A feature is production-integrated only when the manifest traces client ingress,
semantic dispatch, authoritative mutation, continuity, projection, and applicable exact-client MCP
evidence. Codec presence or isolated conformance is insufficient.

Run every batch gate and the universal Rust gates in the plan. Run Goal 02 MCP scenarios for
player-visible changes. Update the ledger and manifest with exact evidence, review source size and
architectural boundaries, then commit the batch with a Conventional Commit subject and this exact
trailer:

Ferrite-Batch: <batch-id>

Do not push, publish, deploy, rewrite history, or open a pull request without separate authorization.
Immediately continue with the next unblocked batch. Mark Goal 03 Complete only after every terminal
checkbox links to committed evidence, the formal gateway runs the composite runtime, migrations and
inspection pass, exact-client scenarios pass, universal gates pass, and the worktree is clean.
```
