version = "26.2"

[inventory]
expected_count = 256
entries_sha1 = "f34b0956b6399c749d4638cd6d3c9226685f41fa"

# This is the independent protocol work queue. Selectors must partition the locked packet report
# exactly once. Broad Todo families are recoverable placeholders and must be split before they can
# become Specified, GatedOptional, NonServerResponsibility, or SourceInconclusive.

[[family]]
id = "PROTO-HANDSHAKE-SERVERBOUND-001"
level = "C0"
state = "handshake"
direction = "serverbound"
patterns = ["*"]
status = "Specified"
responsibility = "Required"
owner = "protocol/handshake-and-status"
specification = "handshake-and-status.md"
evidence = ["OFF-META-001", "OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 0; protocol version signed VarInt, host UTF(255), unsigned big-endian port, intent VarInt 1=status/2=login/3=transfer."]
mappings = ["Status selects a connection-local discovery session; host/port remain untrusted routing context and no C0 field enters simulation or persistence.", "No registry, entity-metadata, data-component, or acknowledgement mapping occurs in handshake."]
transitions = ["Terminal intention switches to status without version comparison, to exact-version login, or to transfer-gated login; invalid intent and unavailable status close."]
ordering = ["The intention is the only handshake packet and must be a complete frame before any packet is decoded under the selected state."]
vectors = ["C0-GOLD-INTENTION-STATUS", "C0-HOST-BOUNDARY", "C0-HOST-MULTIBYTE", "C0-HOST-MALFORMED-UTF8", "C0-PORT-BOUNDARY", "C0-INTENT-ILLEGAL", "C0-STATUS-PROTOCOL-OPAQUE", "C0-STATUS-DISABLED", "C0-TRANSFER-GATE"]
unknowns = []
reproduction = ["Regenerate OFF-REPORT-001, inspect the locked intention codec and server/client transition handlers, then run the named generated-boundary and raw-frame vectors in protocol/conformance.md."]
last_commit = "docs(mc-reference): specify C0 handshake and status"

[[family]]
id = "PROTO-STATUS-SERVERBOUND-001"
level = "C0"
state = "status"
direction = "serverbound"
patterns = ["*"]
status = "Specified"
responsibility = "Required"
owner = "protocol/handshake-and-status"
specification = "handshake-and-status.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 0 status request has no fields; ID 1 ping request has one signed big-endian 64-bit opaque token."]
mappings = ["Status request reads the cached normalized status snapshot without gameplay mutation; ping maps its opaque 64 bits directly to pong.", "No wire registry or metadata mapping occurs in status serverbound."]
transitions = ["First status request marks requested and sends one response; duplicate request closes; ping is legal before or after request, sends one exact echo, then closes."]
ordering = ["Vanilla order is request then response then ping; the server does not require request before ping and has no separate acknowledgement number."]
vectors = ["C0-GOLD-STATUS-REQUEST", "C0-GOLD-PING", "C0-FRAME-PARTIAL", "C0-ID-UNKNOWN", "C0-PACKET-TRAILING", "C0-PING-TRUNCATED", "C0-STATUS-DUPLICATE", "C0-PING-BEFORE-STATUS", "C0-PING-SIGNED-BOUNDARIES", "C0-HAPPY-TRACE"]
unknowns = []
reproduction = ["Regenerate OFF-REPORT-001, inspect the locked status codecs/server listener, and execute the named raw-frame and lifecycle vectors in protocol/conformance.md."]
last_commit = "docs(mc-reference): specify C0 handshake and status"

[[family]]
id = "PROTO-STATUS-CLIENTBOUND-001"
level = "C0"
state = "status"
direction = "clientbound"
patterns = ["*"]
status = "Specified"
responsibility = "Required"
owner = "protocol/handshake-and-status"
specification = "handshake-and-status.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 0 response is UTF(32767) compact JSON with description, optional players/version/favicon and default-false enforcesSecureChat; ID 1 pong is one signed big-endian 64-bit token."]
mappings = ["Normalized status fields project through the locked component, UUID and favicon mappings; pong is an exact token echo.", "C0 has no registry, entity metadata, or data-component numeric mappings."]
transitions = ["Response leaves server status open for ping; pong is sent before server closure. Vanilla client sends ping after its first response, rejects a second response, and closes after pong."]
ordering = ["Required happy order is response before vanilla ping and pong before closure; server echo correlation is bit-exact even though vanilla latency uses its local start time."]
vectors = ["C0-GOLD-STATUS-MINIMAL", "C0-GOLD-STATUS-POPULATED", "C0-GOLD-PONG", "C0-JSON-MINIMAL", "C0-JSON-LENIENT-OPTIONAL", "C0-JSON-ROOT", "C0-JSON-LENGTH", "C0-FAVICON-PRESENTATION", "C0-HAPPY-TRACE"]
unknowns = []
reproduction = ["Generate golden bytes through the locked official response codec, inspect the locked client status listener and icon validator, then execute the named decode and end-to-end vectors in protocol/conformance.md."]
last_commit = "docs(mc-reference): specify C0 handshake and status"

[[family]]
id = "PROTO-LOGIN-SERVERBOUND-REQUIRED-001"
level = "C1"
state = "login"
direction = "serverbound"
patterns = ["minecraft:hello", "minecraft:login_acknowledged"]
status = "Specified"
responsibility = "Required"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 0 hello is UTF(16) name then 16-byte supplied UUID; ID 3 acknowledgement is fieldless and terminal."]
mappings = ["Offline policy ignores supplied UUID and derives UUIDv3 from UTF-8 OfflinePlayer:<exact name>; acknowledgement commits the connection-local configuration transition.", "No login field is a gameplay registry, metadata, or data-component numeric mapping."]
transitions = ["HELLO accepts one valid name then VERIFYING/admission/compression/PROTOCOL_SWITCHING; acknowledgement is legal only in PROTOCOL_SWITCHING and installs both configuration directions."]
ordering = ["Hello follows exact-version login intention; acknowledgement follows terminal login finished and precedes the vanilla brand/client-information configuration packets."]
vectors = ["C1-GOLD-LOGIN-HELLO", "C1-GOLD-LOGIN-ACK-RAW", "C1-GOLD-LOGIN-ACK-C256", "C1-LOGIN-OFFLINE-TRACE", "C1-NAME-BOUNDARIES", "C1-SUPPLIED-UUID-IGNORED", "C1-OFFLINE-UUID-CASE", "C1-HELLO-DUPLICATE", "C1-ACK-EARLY-DUPLICATE", "C1-LOGIN-TIMEOUT"]
unknowns = []
reproduction = ["Encode independent goldens through locked login codecs, inspect server/client login state handlers, and run the named offline identity, illegal-order, timeout, and terminal-transition vectors."]
last_commit = "docs(mc-reference): specify C1 login protocol"

[[family]]
id = "PROTO-LOGIN-SERVERBOUND-OPTIONAL-001"
level = "C4"
state = "login"
direction = "serverbound"
patterns = ["minecraft:key", "minecraft:custom_query_answer", "minecraft:cookie_response"]
status = "GatedOptional"
responsibility = "Optional"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 1 key has two VarInt-length byte arrays; ID 2 custom answer has transaction VarInt plus <=1,048,576-byte discarded remainder; ID 4 cookie response has identifier plus nullable <=5,120-byte array."]
mappings = ["Key maps only inside the online authentication gate; custom query/cookie correlate connection-local transaction/key state and never gameplay state."]
transitions = ["Key is legal only in KEY and enables encryption/authentication; base listener disconnects every custom-query answer and cookie response as unexpected."]
ordering = ["Key follows encryption hello; query/cookie answers require an explicitly owned request, which the base listener never sends."]
vectors = ["C1-ONLINE-GATE", "C1-CUSTOM-QUERY-GATE", "C1-COOKIE-GATE"]
unknowns = []
reproduction = ["Run deterministic RSA/challenge/session-service fixtures and inject bounded query/cookie packets into locked client and base-server listeners as defined in protocol/conformance.md."]
last_commit = "docs(mc-reference): specify C1 login protocol"

[[family]]
id = "PROTO-LOGIN-CLIENTBOUND-REQUIRED-001"
level = "C1"
state = "login"
direction = "clientbound"
patterns = ["minecraft:login_disconnect", "minecraft:login_finished", "minecraft:login_compression"]
status = "Specified"
responsibility = "Required"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 0 disconnect is component JSON UTF(262144); ID 2 terminal finished is bounded game profile plus session UUID; ID 3 compression is threshold VarInt."]
mappings = ["Disconnect projects a connection failure component; finished projects normalized identity and server-session UUID; compression changes only transport state.", "Profile properties use bounded name/value/nullable-signature mapping; login has no gameplay registry mapping."]
transitions = ["Admission failure sends disconnect then closes; nonnegative threshold sends compression before enabling its envelope; finished enters client configuration receive state before acknowledgement."]
ordering = ["Compression negotiation itself is uncompressed and its send callback precedes finished; finished is terminal and acknowledgement uses the matching post-negotiation envelope."]
vectors = ["C1-GOLD-COMPRESSION-256", "C1-GOLD-LOGIN-FINISHED-RAW", "C1-GOLD-LOGIN-FINISHED-C256", "C1-LOGIN-OFFLINE-TRACE", "C1-ADMISSION-DENIED", "C1-COMPRESSION-DISABLED", "C1-COMPRESSION-ZERO", "C1-COMPRESSION-BOUNDARY", "C1-COMPRESSION-DECLARATION", "C1-PROFILE-PROPERTIES", "C1-SESSION-ID"]
unknowns = []
reproduction = ["Generate locked-codec profile/threshold goldens and execute admission, compression, property-bound, session-ID, and terminal-order vectors from protocol/conformance.md."]
last_commit = "docs(mc-reference): specify C1 login protocol"

[[family]]
id = "PROTO-LOGIN-CLIENTBOUND-OPTIONAL-001"
level = "C4"
state = "login"
direction = "clientbound"
patterns = ["minecraft:hello", "minecraft:custom_query", "minecraft:cookie_request"]
status = "GatedOptional"
responsibility = "Optional"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 1 encryption hello has UTF(20) server ID, key/challenge byte arrays and authenticate boolean; ID 4 query has transaction VarInt, identifier and <=1,048,576-byte raw remainder; ID 5 cookie request has identifier."]
mappings = ["Encryption hello selects online authentication; query/cookie requests map only to explicitly owned connection-local extension/transfer state."]
transitions = ["Authentication-enabled server sends hello and enters KEY; base listener never sends query/cookie requests, while vanilla client would answer them with correlated null/nullable values."]
ordering = ["Encryption hello follows login hello and precedes key; query/cookie requests require a pending request before their correlated serverbound answer."]
vectors = ["C1-ONLINE-GATE", "C1-CUSTOM-QUERY-GATE", "C1-COOKIE-GATE"]
unknowns = []
reproduction = ["Drive the locked vanilla client with deterministic encryption, custom-query, and cookie request fixtures, then verify the base server's closed gates."]
last_commit = "docs(mc-reference): specify C1 login protocol"

[[family]]
id = "PROTO-CONFIGURATION-SERVERBOUND-REQUIRED-001"
level = "C1"
state = "configuration"
direction = "serverbound"
patterns = ["minecraft:client_information", "minecraft:custom_payload", "minecraft:finish_configuration", "minecraft:keep_alive", "minecraft:pong", "minecraft:select_known_packs"]
status = "Specified"
responsibility = "Required"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 0/2/3/4/5/7 are bounded client information, channel plus payload, terminal unit, signed long, signed int, and <=64 known-pack list respectively."]
mappings = ["Client information becomes the latest connection cookie; brand is ignored by the base server; known packs select only registry data elision; liveness tokens and finish remain connection-local.", "Registry/tag acknowledgement and play transition never expose packet structs or numeric IDs to world persistence."]
transitions = ["Known-pack selection is legal only in synchronize_registries; exact pending keepalive echo clears the challenge; finish is legal only in join_world and installs play after registry/spawn readiness."]
ordering = ["Brand/client information may interleave with server start; known-pack response precedes registry/tag completion; terminal finish follows server finish and precedes all serverbound play packets."]
vectors = ["C1-GOLD-CONFIG-CLIENT-INFO", "C1-GOLD-CONFIG-BRAND-SB", "C1-GOLD-CONFIG-KNOWN-RESPONSE", "C1-GOLD-CONFIG-FINISH", "C1-GOLD-CONFIG-KEEPALIVE", "C1-GOLD-CONFIG-PONG", "C1-CONFIG-HAPPY-TRACE", "C1-CONFIG-KNOWN-ORDER", "C1-CONFIG-FINISH-ORDER", "C1-CONFIG-KEEPALIVE-STATE", "C1-CONFIG-CLIENT-INFO-BOUNDARIES", "C1-CONFIG-CUSTOM-PAYLOAD-BOUNDS"]
unknowns = []
reproduction = ["Generate official-codec frames and replay the minimum offline task queue, known-pack equality branches, client-information bounds, liveness challenge states, and terminal transition vectors in protocol/conformance.md."]
last_commit = "docs(mc-reference): specify C1 configuration protocol"

[[family]]
id = "PROTO-CONFIGURATION-SERVERBOUND-OPTIONAL-001"
level = "C4"
state = "configuration"
direction = "serverbound"
patterns = ["minecraft:cookie_response", "minecraft:resource_pack", "minecraft:custom_click_action", "minecraft:accept_code_of_conduct"]
status = "GatedOptional"
responsibility = "Optional"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 1 is identifier plus nullable <=5,120-byte value; ID 6 is UUID plus action 0..7; ID 8 is identifier plus <=65,536-byte optional NBT; ID 9 is unit."]
mappings = ["Cookie/resource-pack/code-of-conduct values are gated connection tasks; custom click maps only to its explicitly server-owned handler."]
transitions = ["Base server rejects cookie response; resource-pack terminal action and conduct accept advance only their matching current task; custom click dispatches without advancing C1."]
ordering = ["Every response requires its owned request/task; unsolicited or wrong-task acknowledgement is a configuration fault, except custom click which is an independently registered action."]
vectors = ["C1-CONFIG-COOKIE-GATE", "C1-CONFIG-RESOURCE-PACK-GATE", "C1-CONFIG-CODE-OF-CONDUCT-GATE", "C1-CONFIG-CUSTOM-CLICK-GATE"]
unknowns = []
reproduction = ["Inject bounded optional responses before, during, and after their locked tasks and verify refusal, UUID/action, NBT, acceptance, and advancement behavior."]
last_commit = "docs(mc-reference): specify C1 configuration protocol"

[[family]]
id = "PROTO-CONFIGURATION-CLIENTBOUND-REQUIRED-001"
level = "C1"
state = "configuration"
direction = "clientbound"
patterns = ["minecraft:custom_payload", "minecraft:disconnect", "minecraft:finish_configuration", "minecraft:keep_alive", "minecraft:ping", "minecraft:registry_data", "minecraft:update_enabled_features", "minecraft:update_tags", "minecraft:select_known_packs"]
status = "Specified"
responsibility = "Required"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 1/2/3/4/5/7/12/13/14 carry bounded payload, component NBT, terminal unit, signed long/int, ordered registry entries, feature identifiers, tag ID maps, and known-pack descriptors."]
mappings = ["Registry entry order defines dynamic wire IDs; tag member VarInts index matching registries; enabled features and known-pack data elision remain connection projection state.", "Brand becomes client telemetry/presentation; disconnect, liveness, and finish do not mutate simulation or persistence."]
transitions = ["Offer starts synchronize_registries; registry/tag packets build client registry access; finish finalizes it and installs clientbound play before acknowledgement; keepalive/ping require exact echoes."]
ordering = ["Brand/features precede offer; response triggers registries then tags; spawn readiness precedes terminal finish; finish precedes all clientbound play packets."]
vectors = ["C1-GOLD-CONFIG-BRAND-CB", "C1-GOLD-CONFIG-FEATURES", "C1-GOLD-CONFIG-KNOWN-OFFER", "C1-GOLD-CONFIG-REGISTRY", "C1-GOLD-CONFIG-TAGS", "C1-GOLD-CONFIG-FINISH", "C1-GOLD-CONFIG-KEEPALIVE", "C1-GOLD-CONFIG-PING", "C1-CONFIG-HAPPY-TRACE", "C1-CONFIG-KNOWN-ORDER", "C1-CONFIG-REGISTRY-BOUNDARIES", "C1-CONFIG-TAG-MAPPINGS", "C1-CONFIG-FEATURE-NAMES", "C1-CONFIG-FINISH-ORDER", "C1-CONFIG-KEEPALIVE-STATE", "C1-CONFIG-CUSTOM-PAYLOAD-BOUNDS"]
unknowns = []
reproduction = ["Generate official-codec goldens, reconstruct all 29 synchronized registries and tag ID maps, then execute happy, malformed, duplicate, timeout, and terminal-order vectors."]
last_commit = "docs(mc-reference): specify C1 configuration protocol"

[[family]]
id = "PROTO-CONFIGURATION-CLIENTBOUND-OPTIONAL-001"
level = "C4"
state = "configuration"
direction = "clientbound"
patterns = ["minecraft:cookie_request", "minecraft:reset_chat", "minecraft:resource_pack_pop", "minecraft:resource_pack_push", "minecraft:store_cookie", "minecraft:transfer", "minecraft:custom_report_details", "minecraft:server_links", "minecraft:clear_dialog", "minecraft:show_dialog", "minecraft:code_of_conduct"]
status = "GatedOptional"
responsibility = "Optional"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 0/6/8..11/15..19 use the exact cookie, unit, resource-pack, transfer, bounded report/link, context-free dialog, and UTF code-of-conduct schemas in the specification."]
mappings = ["Every optional packet affects connection transfer, client resources, reporting metadata, or presentation only; none enters authoritative world state."]
transitions = ["Base fresh configuration omits every gate except configured server links/tasks; transfer closes and reconnects, conduct/resource pack can block, and reset/dialog packets alter client presentation."]
ordering = ["Cookie/resource-pack/conduct replies correlate their request/task; transfer may preserve stored cookies; reset-chat is for play-to-configuration re-entry, not the fresh minimum path."]
vectors = ["C1-CONFIG-COOKIE-GATE", "C1-CONFIG-RESOURCE-PACK-GATE", "C1-CONFIG-CODE-OF-CONDUCT-GATE", "C1-CONFIG-TRANSFER-GATE", "C1-CONFIG-PRESENTATION-GATES"]
unknowns = []
reproduction = ["Drive the locked vanilla client through each optional packet at codec boundaries and verify request correlation, task blocking, transfer, URI validation, and presentation-only effects."]
last_commit = "docs(mc-reference): specify C1 configuration protocol"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-ENTRY-001"
level = "C1"
state = "play"
direction = "serverbound"
patterns = ["minecraft:accept_teleportation"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 0 is one teleport challenge VarInt with no additional fields."]
mappings = ["A matching challenge acknowledges only connection-local position synchronization; the numeric challenge never enters authoritative movement, ECS state or persistence."]
transitions = ["A matching ID clears the one pending correction after snapping authoritative position; a stale nonmatching ID is ignored; a duplicate matching ID after clearance disconnects as invalid movement."]
ordering = ["The vanilla client applies ID-72 player_position, sends ID-0 accept_teleportation, then sends ID-31 move_player_pos_rot; the server resends a pending correction after more than 20 listener ticks."]
vectors = ["C1-GOLD-PLAY-ACCEPT", "C1-PLAY-ENTRY-TRACE", "C1-PLAY-TELEPORT-ORDER", "C1-PLAY-TELEPORT-STALE-DUPLICATE"]
unknowns = []
reproduction = ["Generate the official-codec acknowledgement frame and replay matching, reordered, stale, duplicate and resend cases from the first play position."]
last_commit = "docs(mc-reference): specify C1 play entry"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-MOVEMENT-001"
level = "C2"
state = "play"
direction = "serverbound"
patterns = ["minecraft:chunk_batch_received", "minecraft:client_tick_end", "minecraft:client_information", "minecraft:keep_alive", "minecraft:move_player_pos", "minecraft:move_player_pos_rot", "minecraft:move_player_rot", "minecraft:move_player_status_only", "minecraft:move_vehicle", "minecraft:paddle_boat", "minecraft:player_abilities", "minecraft:player_command", "minecraft:player_input", "minecraft:player_loaded", "minecraft:pong"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 11/13/14/28/30..35/40/42..45 use the exact float, unit, client-information, signed echo, four-form player movement, vehicle, paddle, ability, command, input and terrain-ready grammars in the specification."]
mappings = ["Accepted movement becomes a normalized authoritative collision move; client options and input become bounded session state; chunk advice, liveness, tick boundaries, raw flags, packet/entity IDs and echo payloads remain adapter-local.", "Player/vehicle correction thresholds, exceptional-float handling, collision recovery, floating timeout and idle/known-movement updates reproduce the locked handlers without importing wire structs into ECS persistence."]
transitions = ["ID 44 or expiry opens the 60-tick client-load gate; matching keepalive clears its pending challenge while invalid remote echoes disconnect; chunk feedback acknowledges/floors batch count and expands the scheduling window; client_tick_end closes one known-movement interval."]
ordering = ["The vanilla client sends changed input before movement, one selected player-movement form per active tick (or riding rotation/vehicle), then client_tick_end; player_loaded follows terrain readiness; chunk feedback follows batch-finished; keepalive and ping use disjoint exact echoes."]
vectors = ["C2-GOLD-SERVERBOUND-MOVEMENT", "C2-MOVEMENT-FORMS-CADENCE", "C2-MOVEMENT-VALIDATION", "C2-MOVEMENT-TELEPORT-LOAD-GATES", "C2-MOVEMENT-FLOATING", "C2-VEHICLE-CORRECTION", "C2-INPUT-COMMAND-ABILITIES", "C2-CLIENT-TICK-END", "C2-TERRAIN-READY", "C2-CHUNK-BATCH-FEEDBACK", "C2-PLAY-LIVENESS"]
unknowns = []
reproduction = ["Generate every fixture with the locked Java 25 official codec, trace client movement selection and server validation across load/teleport/collision/speed/non-finite/floating cases, and drive chunk/liveness feedback at every boundary."]
last_commit = "docs(mc-reference): specify C2 movement session"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-BLOCK-001"
level = "C2"
state = "play"
direction = "serverbound"
patterns = ["minecraft:pick_item_from_block", "minecraft:player_action", "minecraft:swing", "minecraft:use_item_on", "minecraft:use_item"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 36/41/63/66/67 use packed block position plus boolean, action/position/modulo-direction/sequence, strict hand, hand/block-hit/sequence, and hand/sequence/yaw/pitch grammars exactly as specified."]
mappings = ["Destroy/use requests map to normalized authoritative interaction commands paired with the source-specified block/item mechanics; pick-block maps a namespaced clone stack into inventory selection; swing and auxiliary actions map only their documented hand/player intent.", "Packed coordinates, enum ordinals, hit offsets, sequence counters, raw item/component identities, correction packet forms and client prediction records remain inside the 26.2 adapter and do not enter ECS persistence."]
transitions = ["Destroy/use predictive requests act only after the client-load gate and register a nonnegative sequence at their path-specific point; auxiliary actions use the loaded gate without ACK; pick and swing have their explicit no-load-gate behavior; every packet is legal only in play."]
ordering = ["Prediction runs before request send; use-on/use register before validation while destroy registers after its handler; the server coalesces the maximum only until next listener tick; immediate use-on/break corrections precede ACK while ordinary successful block deltas may follow it."]
vectors = ["C2-GOLD-SERVERBOUND-BLOCK", "C2-BLOCK-CODEC-BOUNDARIES", "C2-BLOCK-SEQUENCE-LIFECYCLE", "C2-BLOCK-PREDICTION-ORDER", "C2-BLOCK-USE-ON-ADMISSION", "C2-BLOCK-SERVER-SWING-QUIRK", "C2-BLOCK-BREAK-ACTIONS", "C2-BLOCK-AUXILIARY-ACTIONS", "C2-BLOCK-PICK", "C2-BLOCK-USE-IN-AIR", "C2-BLOCK-END-TO-END"]
unknowns = []
reproduction = ["Generate all five goldens with the locked Java 25 codecs, trace client prediction and server ACK registration at every load/validation boundary, then replay pick, auxiliary, break, use-on, use-in-air, correction and exceptional-value vectors against the locked endpoints."]
last_commit = "docs(mc-reference): specify C2 block convergence"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-CHAT-001"
level = "C3"
state = "play"
direction = "serverbound"
patterns = ["minecraft:chat_ack", "minecraft:chat_command", "minecraft:chat_command_signed", "minecraft:chat", "minecraft:chat_session_update", "minecraft:command_suggestion"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 6..10/15 carry signed offset VarInt; default UTF command; default UTF command plus epoch-millisecond long, salt long, up-to-eight named fixed signatures and 20-slot update; UTF(256) chat plus epoch-millisecond long, salt long, nullable fixed signature and update; UUID plus expiry/public-key/signature data; and transaction VarInt plus UTF(32500), respectively."]
mappings = ["Accepted chat maps to a normalized filtered/decorated player message, accepted commands to the command dispatcher with normalized signed-argument context, and suggestions to an ephemeral completion request; raw strings, salts, signature bytes, argument names, transaction IDs and packet IDs remain adapter-local.", "The 20-slot last-seen window, signature cache, SHA256withRSA chain index, chat session/key, async filter chain and independent chat/command throttlers are connection-local protocol state; none becomes world or ECS persistence identity."]
transitions = ["Before a validated session, offline/non-enforcing connections use the unsigned decoder and accept null chat/argument signatures; an authenticated session replaces the decoder only after key validation, rejects rollback/invalid key updates as specified, and publishes chat initialization after prior filter-chain work.", "Invalid acknowledgement offsets, bit windows or checksums disconnect for chat validation; illegal characters disconnect; signature/key/chain failures send the documented red system error and may break the chain; chat-hidden refusal and secure unsigned-command refusal do not execute their payloads."]
ordering = ["Chat and signed command apply their embedded last-seen update before character/admission handling; accepted work is main-thread scheduled, chat filtering/decorating/broadcast is serialized by the connection future chain, and a session publication is appended behind prior chat work.", "The client clears accumulated acknowledgement offset into each chat/signed-command update or a standalone ID 6 after offset exceeds 64; suggestions increment a client transaction, cancel the prior future and accept only the matching response while the server caps the returned list at 1,000."]
vectors = ["C3-GOLD-SERVERBOUND-CHAT", "C3-CHAT-CODECS", "C3-CHAT-LAST-SEEN", "C3-CHAT-SESSION-SECURITY", "C3-CHAT-SIGNATURE-CHAIN", "C3-CHAT-ADMISSION-FILTERING", "C3-COMMAND-SIGNING-DISPATCH", "C3-CHAT-SPAM-THROTTLING", "C3-COMMAND-SUGGESTION-CORRELATION", "C3-CHAT-ORDER", "C3-CHAT-END-TO-END"]
unknowns = []
reproduction = ["Generate all six goldens through the locked Java 25 codecs, then replay UTF/count/bit-window/key/signature bounds, offline and enforced-secure decoders, chain/order/checksum failures, visibility/filter/spam gates, command argument matching and stale suggestion correlations against the locked client/server handlers."]
last_commit = "docs(mc-reference): specify C3 serverbound chat"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-ENTITY-SESSION-001"
level = "C3"
state = "play"
direction = "serverbound"
patterns = ["minecraft:attack", "minecraft:client_command", "minecraft:interact", "minecraft:pick_item_from_entity", "minecraft:spectator_action", "minecraft:teleport_to_entity"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 1/12/26/37/62/64 are entity VarInt; strict action VarInt; entity/zero-fallback hand/LP-vector/boolean; entity/boolean; optional entity VarInt; and UUID respectively, exactly as specified."]
mappings = ["Attack/interact/pick map level-local target numbers to current entities before normalized combat, interaction or inventory commands; client commands map to respawn/stat/gamerule session requests; spectator actions map to camera selection or UUID-targeted authoritative teleport.", "Entity IDs, compact-vector quantization, action/hand ordinals, optional-ID bias, UUID lookup form, packet IDs and response packet structs remain in the 26.2 adapter; authoritative entities, items, damage and respawn state use namespaced/domain identities."]
transitions = ["Attack/interact and near-camera selection require the client-loaded and documented mode/range gates; pick has no client-loaded gate; UUID teleport requires spectator only; client command always resets idle and branches to dead/win respawn, dirty statistics or permission-gated gamerules."]
ordering = ["The vanilla client sends carried-slot convergence before attack/interact, predicts those actions locally after send, and uses no acknowledgement counter; camera selection relocates before set-camera publication, while respawn command leads to respawn then position and state reprojection."]
vectors = ["C3-GOLD-SERVERBOUND-ENTITY-SESSION", "C3-ENTITY-INGRESS-CODECS", "C3-ATTACK-ADMISSION", "C3-INTERACT-ADMISSION", "C3-PICK-ENTITY", "C3-SPECTATOR-CAMERA", "C3-CLIENT-COMMAND", "C3-ENTITY-SESSION-END-TO-END"]
unknowns = []
reproduction = ["Generate all six goldens through the locked Java 25 codecs, then replay compact-vector, enum, reach, mode, permission, invalid-target, respawn and response-order boundaries against the locked client/server handlers."]
last_commit = "docs(mc-reference): specify C3 entity session"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-CONTAINER-CONVERGENCE-001"
level = "C3"
state = "play"
direction = "serverbound"
patterns = ["minecraft:container_button_click", "minecraft:container_click", "minecraft:container_close", "minecraft:container_slot_state_changed", "minecraft:set_carried_item"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 17/18/19/20/53 carry container/button VarInts; container/state VarInts, signed slot short/button byte, zero-fallback input, bounded hashed slot map and cursor hash; container VarInt; slot/container VarInts plus boolean; and signed selected-slot short exactly as specified."]
mappings = ["Click inputs map to the seven source-specified deterministic menu operations; item/component holders use the locked 1,537-item and 111-component registries, and hashes are registry-aware CRC32C evidence only.", "Ferrite maps admitted traffic to normalized current-menu commands; container/state IDs, slot/button widths, raw registry/input IDs, hashes and remote snapshots remain version-local connection state."]
transitions = ["The client predicts first and hashes changed slots/cursor; the server gates current menu, player state, validity and slot, executes authoritatively even when state is stale, then uses hashes only to suppress matching corrections; buttons, crafter state, close and carried selection follow their independent gates."]
ordering = ["A stale state executes then forces content/cursor and all-data resynchronization; a matching state executes then publishes slot deltas, cursor and data, with remote updates suppressed across the transaction; close IDs are ignored by both handlers and valid carried selection resets idle."]
vectors = ["C3-GOLD-SERVERBOUND-CONTAINER", "C3-CONTAINER-INGRESS-CODECS", "C3-CONTAINER-HASHED-STACKS", "C3-CONTAINER-CLICK-PREDICTION", "C3-CONTAINER-CLICK-ADMISSION", "C3-CONTAINER-CONTROLS", "C3-CONTAINER-CLOSE", "C3-CARRIED-SELECTION", "C3-CONTAINER-CONVERGENCE-END-TO-END"]
unknowns = []
reproduction = ["Generate all five goldens through the locked Java 25 codecs, then replay signed widths, collection limits, registry/hash collisions, every click input, current/stale state, player/menu gates, close races and correction ordering against locked client/server handlers."]
last_commit = "docs(mc-reference): specify C3 container convergence"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-SIGN-UPDATE-001"
level = "C3"
state = "play"
direction = "serverbound"
patterns = ["minecraft:sign_update"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 61 carries packed signed block position, front/back boolean and exactly four strings with no count; receive uses UTF(384), malformed UTF-8 replacement and a 1,152-byte cap, while the official member encoder asymmetrically uses default UTF(32767/98,301 bytes)."]
mappings = ["The listener strips legacy formatting, asynchronously filters all four lines and retains prior per-line style while producing filtered-only or raw-plus-filtered literal forms according to player filtering policy.", "Ferrite maps packed position and side into an authorized normalized sign mutation; wire IDs, allowed-editor UUID state, filter futures and client presentation forms remain adapter-local."]
transitions = ["At filter completion the current level must have the chunk and sign; the sign must be unwaxed, leveled and authorized for this player UUID. Success rebuilds one side and calls flags-3 update while marking changed, clears authorization, then makes an unconditional second flags-3 update call even for semantically unchanged text; rejection has no response and does not clear authorization."]
ordering = ["Every normal vanilla editor removal sends once; the server strips then filters before resetting idle and checking completion-time world/authorization state, so concurrent range-tick, wax, block, level and editor changes participate in admission."]
vectors = ["C3-GOLD-SERVERBOUND-SIGN-UPDATE", "C3-SIGN-UPDATE-CODEC", "C3-SIGN-EDITOR-SUBMIT", "C3-SIGN-UPDATE-ADMISSION", "C3-SPECIAL-SCREENS-ORDER", "C3-SPECIAL-SCREENS-END-TO-END"]
unknowns = []
reproduction = ["Generate the golden through the locked Java 25 codec, then replay all UTF/coordinate/side boundaries, every editor exit, format stripping/filter timing, sign/chunk/wax/editor/range races and accepted block-entity convergence against the locked handlers."]
last_commit = "docs(mc-reference): specify C3 special screens"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-RECIPE-BOOK-001"
level = "C3"
state = "play"
direction = "serverbound"
patterns = ["minecraft:place_recipe", "minecraft:recipe_book_change_settings", "minecraft:recipe_book_seen_recipe"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 39/46/47 carry signed container/display VarInts plus a boolean; strict recipe-book-type ordinal VarInt plus open/filtering booleans; and one signed display VarInt, respectively."]
mappings = ["Recipe display IDs are feature-filtered indices into the current RecipeManager display list, not registry IDs or recipe resource keys; a valid index resolves both its display payload and namespaced parent recipe, while book types strictly map 0..3 to crafting/furnace/blast-furnace/smoker.", "Ferrite maps valid requests to normalized current-menu placement, per-player recipe-book settings or parent-recipe highlight state; display indices, menu IDs, ordinals, GUI flags and placement helpers remain version-local."]
transitions = ["Placement resets idle before spectator/container/menu/validity/display/unlock/placeability gates, then either mutates the current recipe menu, does nothing, or sends an immediate ghost display after clearing an uncraftable grid; settings directly replace two booleans; seen removes the valid display's parent recipe highlight."]
ordering = ["The vanilla client updates local settings/highlights before sending their tokenless requests; placement predicts no inventory result, and only the cannot-craft branch has an immediate ID-63 response, which precedes ordinary later container slot convergence."]
vectors = ["C3-GOLD-SERVERBOUND-RECIPE-BOOK", "C3-RECIPE-BOOK-CODECS", "C3-RECIPE-DISPLAY-ID-MAPPING", "C3-RECIPE-PLACE-ADMISSION", "C3-RECIPE-PLACE-MUTATION", "C3-RECIPE-BOOK-SETTINGS", "C3-RECIPE-BOOK-HIGHLIGHTS", "C3-RECIPE-BOOK-ORDER", "C3-RECIPE-BOOK-END-TO-END"]
unknowns = []
reproduction = ["Generate all three goldens through the locked Java 25 codecs, rebuild the feature-filtered display map, then replay signed/enum/boolean boundaries, current-menu and unlock gates, inventory-capacity/craftability branches, settings and multi-display highlight behavior against the locked handlers."]
last_commit = "docs(mc-reference): specify C3 recipe book"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-MERCHANT-001"
level = "C3"
state = "play"
direction = "serverbound"
patterns = ["minecraft:select_trade"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 51 carries one unrestricted signed VarInt selection hint; it has no container ID, state ID, stack, boolean or acknowledgement field."]
mappings = ["The hint addresses the current MerchantOffers list: hint zero, negative and at-or-beyond-size values make result lookup scan from the first matching offer, while a positive in-range hint tests only that offer; payment auto-fill separately requires any in-range index including zero.", "Ferrite maps an admitted request to normalized current merchant-menu selection and payment auto-fill; the signed hint, menu-local offer order, GUI scroll position and payment/result slots remain adapter-local."]
transitions = ["The server requires only a current still-valid MerchantMenu, stores every signed hint and recomputes the result before range-checking auto-fill; a valid index returns payment stacks to inventory in non-atomic slot order and then fills exact-cost matches, while invalid indices retain their hint/result mutation and move no payment."]
ordering = ["The vanilla screen applies the same hint and auto-fill locally before sending ID 51; the server repeats it authoritatively and sends no direct response, so ordinary later ID 20/96/19 container publication performs convergence."]
vectors = ["C3-GOLD-SERVERBOUND-MERCHANT", "C3-MERCHANT-CODECS", "C3-MERCHANT-SELECTION-ADMISSION", "C3-MERCHANT-SELECTION-LOOKUP", "C3-MERCHANT-AUTOFILL", "C3-MERCHANT-ORDER", "C3-MERCHANT-END-TO-END"]
unknowns = []
reproduction = ["Generate the golden through the locked Java 25 codec, then replay every signed selection, current-menu/validity branch, matching/swapped/out-of-stock offer, full/partial payment return, exact-component auto-fill and local/server convergence against the locked menu and listeners."]
last_commit = "docs(mc-reference): specify C3 merchant trading"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-ANVIL-BEACON-001"
level = "C3"
state = "play"
direction = "serverbound"
patterns = ["minecraft:rename_item", "minecraft:set_beacon"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 48/52 carry default UTF(32767 UTF-16 units/98301 bytes) name; and independently optional strict configured mob-effect holders as presence boolean plus raw VarInt, respectively; neither carries a container/state/sequence field."]
mappings = ["Rename filters section sign, control characters below space and DEL before its 50-UTF-16-unit semantic bound, then maps blank to absent literal custom name or nonblank to a literal custom-name component while the owned anvil rule recomputes result/cost.", "Beacon packet holders resolve through the locked 40-effect configured registry; server validation separately admits the six tiered beacon effects, current signed level and payment state, then writes choices through the distinct built-in effect-ID-plus-one menu-data domain into normalized block-entity holders.", "Ferrite maps both requests against the current normalized menu transaction; raw UTF, registry/data IDs, GUI choices and container slots remain adapter-local."]
transitions = ["Rename requires only a current still-valid AnvilMenu; a filtered over-50 or unchanged value is a no-op, while a changed value updates name state, result custom-name presentation, reruns the source-specified anvil computation and broadcasts ordinary deltas.", "Beacon requires only a current still-valid BeaconMenu; success validates tier/equality, stores both choices, consumes one existing payment, may play selection sound and marks the chunk unsaved; false disconnects generically, while absent-primary plus a tier-1..3 secondary at level four throws through the locked null-equality quirk."]
ordering = ["The anvil predicts each effective edit before sending and receives ordinary result/cost convergence without a token; canonical beacon Done sends ID 52 then locally emits ordinary close ID 19, so selection/consumption precede close, while Cancel/Escape send only close."]
vectors = ["C3-GOLD-SERVERBOUND-ANVIL-BEACON", "C3-ANVIL-BEACON-CODECS", "C3-ANVIL-RENAME-PREDICTION", "C3-ANVIL-RENAME-ADMISSION", "C3-BEACON-EFFECT-MAPPING", "C3-BEACON-SELECTION-ADMISSION", "C3-ANVIL-BEACON-ORDER", "C3-ANVIL-BEACON-END-TO-END"]
unknowns = []
reproduction = ["Generate both goldens through the locked Java 25 codecs, then replay UTF/filter/length/current-menu boundaries, local anvil prediction and result broadcast, every optional/effect/tier/payment/equality branch including the null-primary exception, and beacon selection-before-close ordering against locked handlers."]
last_commit = "docs(mc-reference): specify C3 anvil and beacon controls"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-INVENTORY-AUXILIARY-001"
level = "C3"
state = "play"
direction = "serverbound"
patterns = ["minecraft:bundle_item_selected", "minecraft:edit_book", "minecraft:seen_advancements"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 3/24/50 carry two signed VarInts; inventory-slot VarInt plus up to 100 UTF(1024) pages and optional UTF(32) title; and action enum VarInt plus an OPENED_TAB-only default-bounded resource identifier, respectively."]
mappings = ["Bundle menu-slot/index values select transient BundleContents state on the current menu stack; the selected index guides the next owned secondary-click removal but is excluded from component equality, save and network codecs.", "Book hotbar/offhand slots map filtered pages to writable_book_content or transmute the current writable book to written_book_content with filtered title/pages, player author, generation zero and resolved true; item/container identifiers remain normalized registry/component state.", "An OPENED_TAB identifier resolves through the current data-pack advancement manager and only a displayed root becomes the runtime PlayerAdvancements selection; the packet identifier and selection are not advancement-progress persistence identity."]
transitions = ["Bundle selection toggles the same valid index off, switches to a different in-range content index, or clears for -1/out-of-content values; an invalid menu slot or missing bundle component is a no-op, while values below -1 fault decoding.", "Book packets outside hotbar 0..8/offhand 40 are ignored; after asynchronous filtering the callback re-reads that inventory slot and mutates only a stack still carrying writable_book_content, with absent title editing and present title finalizing the book.", "Unknown advancement IDs and CLOSED_SCREEN are no-ops; known non-root/root-without-display opens normalize selection to null, while a changed valid displayed root sends the paired clientbound selection correction."]
ordering = ["The bundle client mutates its local component before ID 3 and the server independently mutates handler-time current-menu state without a token or explicit broadcast; subsequent owned container clicks consume the selection.", "Each ID 24 text-filter future runs independently and later schedules on the server executor, so completion order—not arrival order—wins and slot replacement races are intentional; ordinary inventory projection is the only convergence response.", "Advancement-screen initialization/clicks send OPENED_TAB before local selection notification, removal always sends ignored CLOSED_SCREEN, and the server retains its unsaved last selected root until a later valid/normalizing open or advancement reload."]
vectors = ["C3-GOLD-SERVERBOUND-INVENTORY-AUXILIARY", "C3-INVENTORY-AUX-CODECS", "C3-BUNDLE-SELECTION-PREDICTION", "C3-BUNDLE-SELECTION-ADMISSION", "C3-BOOK-EDIT-ADMISSION", "C3-BOOK-FILTER-ORDER", "C3-BOOK-FINALIZATION", "C3-ADVANCEMENT-TAB-CORRELATION", "C3-INVENTORY-AUX-ORDER", "C3-INVENTORY-AUX-END-TO-END"]
unknowns = []
reproduction = ["Generate the four goldens through the locked Java 25 codecs, then replay VarInt/string/list/optional/enum/identifier bounds, visible and hidden bundle indices, current-menu/slot races, independent book-filter completions, every writable/finalized book branch, and known/unknown/root/child/display/close advancement selections against the locked client/server handlers."]
last_commit = "docs(mc-reference): specify C3 inventory auxiliary requests"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-ADMIN-001"
level = "C4"
state = "play"
direction = "serverbound"
patterns = ["minecraft:block_entity_tag_query", "minecraft:change_difficulty", "minecraft:change_game_mode", "minecraft:debug_subscription_request", "minecraft:entity_tag_query", "minecraft:jigsaw_generate", "minecraft:lock_difficulty", "minecraft:set_command_block", "minecraft:set_command_minecart", "minecraft:set_creative_mode_slot", "minecraft:set_game_rule", "minecraft:set_jigsaw_block", "minecraft:set_structure_block", "minecraft:set_test_block", "minecraft:test_instance_block_action"]
status = "Todo"
responsibility = "Optional"
owner = "protocol/play-serverbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify permission-gated debug, operator, creative, command-block, structure, jigsaw, test and gamerule requests and their refusal behavior."]
reproduction = ["Trace each locked administrative codec and permission/feature branch with unprivileged and privileged players."]
last_commit = ""

[[family]]
id = "PROTO-PLAY-SERVERBOUND-OPTIONAL-001"
level = "C4"
state = "play"
direction = "serverbound"
patterns = ["minecraft:configuration_acknowledged", "minecraft:cookie_response", "minecraft:custom_payload", "minecraft:ping_request", "minecraft:resource_pack", "minecraft:custom_click_action"]
status = "Todo"
responsibility = "Optional"
owner = "protocol/play-serverbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify play-to-configuration acknowledgement, cookies, payloads, diagnostics, resource packs and custom-click gates with common-listener reuse."]
reproduction = ["Trace locked common/play listeners and state switches for every optional request, response and unsolicited input."]
last_commit = ""

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-ENTRY-001"
level = "C1"
state = "play"
direction = "clientbound"
patterns = ["minecraft:change_difficulty", "minecraft:commands", "minecraft:entity_event", "minecraft:game_event", "minecraft:initialize_border", "minecraft:login", "minecraft:player_abilities", "minecraft:player_info_update", "minecraft:player_position", "minecraft:recipe_book_add", "minecraft:recipe_book_settings", "minecraft:server_data", "minecraft:set_default_spawn_position", "minecraft:set_held_slot", "minecraft:set_time", "minecraft:ticking_state", "minecraft:ticking_step", "minecraft:update_recipes"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 10/16/34/38/43/49/64/70/72/74/76/86/97/105/113/127/128/133 use the exact join-state, command-tree, event, border, registry-aware login, ability, player-list, position, recipe, server-data, spawn, clock and tick grammars in the specification."]
mappings = ["Dynamic dimension-type/world-clock IDs and static item, recipe-display, slot-display, recipe-category and command-argument IDs resolve through the locked registries; player/entity IDs and teleport IDs remain session-local.", "Join projection initializes client-only level, permissions, recipes, player list, border, clocks, spawn and presentation without importing packet structs or numeric IDs into simulation or persistence."]
transitions = ["Login creates the client level; player_position creates one teleport challenge; level-chunks-load-start opens the terrain readiness gate; ticking state/step synchronize the client clock controller."]
ordering = ["The locked server emits login first, core join projections in PlayerList order, position before its acknowledgement, then border/time/spawn/load-start/ticking state and step before asynchronous chunk batches."]
vectors = ["C1-GOLD-PLAY-LOGIN-MINIMAL", "C1-GOLD-PLAY-DIFFICULTY", "C1-GOLD-PLAY-ABILITIES", "C1-GOLD-PLAY-HELD", "C1-GOLD-PLAY-COMMANDS-EMPTY", "C1-GOLD-PLAY-RECIPE-SETTINGS", "C1-GOLD-PLAY-RECIPE-ADD-EMPTY", "C1-GOLD-PLAY-RECIPES-EMPTY", "C1-GOLD-PLAY-PLAYER-INFO-EMPTY", "C1-GOLD-PLAY-POSITION-ZERO", "C1-GOLD-PLAY-GAME-LOAD-START", "C1-GOLD-PLAY-BORDER-DEFAULT", "C1-GOLD-PLAY-SPAWN", "C1-GOLD-PLAY-TIME-EMPTY", "C1-GOLD-PLAY-TICKING", "C1-GOLD-PLAY-TICK-STEP", "C1-PLAY-ENTRY-TRACE", "C1-PLAY-LOGIN-BOUNDARIES", "C1-PLAY-COMMAND-TREE", "C1-PLAY-PLAYER-INFO", "C1-PLAY-RECIPE-STATE", "C1-PLAY-TELEPORT-ORDER"]
unknowns = []
reproduction = ["Generate official-codec default/empty frames, trace a new offline player through PlayerList placement, and replay malformed registry IDs, trees, lists, events and teleport order against the locked client."]
last_commit = "docs(mc-reference): specify C1 play entry"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-TERRAIN-001"
level = "C2"
state = "play"
direction = "clientbound"
patterns = ["minecraft:bundle_delimiter", "minecraft:chunk_batch_finished", "minecraft:chunk_batch_start", "minecraft:chunks_biomes", "minecraft:forget_level_chunk", "minecraft:level_chunk_with_light", "minecraft:light_update", "minecraft:set_chunk_cache_center", "minecraft:set_chunk_cache_radius", "minecraft:set_simulation_distance"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 0/11..13/37/45/48/94/95/111 use the exact unit delimiter, batch, bounded biome-list, packed unload, dimension-sized chunk/palette/block-entity/light, incremental light and signed cache-control grammars in the specification."]
mappings = ["Block-state raw IDs resolve through the locked 32,366-state table, biome IDs through the frozen configuration registry, block-entity IDs through the 49-entry static registry, and heightmap IDs through the locked enum/fallback rules in registry-and-metadata-mappings.md.", "Chunk snapshots, palette data, heightmaps, block-entity tags and lighting project authoritative terrain into client cache/render state; raw IDs, masks, indices, batch counters and cache coordinates remain version-adapter state."]
transitions = ["Bundle delimiters create one bounded synthetic bundle; cache controls change view projection; start/full-chunk/finish plus serverbound feedback advances batch flow; full chunk installs cache and queues light; biome refresh replaces present chunk biomes; forget drops chunk/debug/light state; compiled terrain or readiness exemptions trigger player_loaded."]
ordering = ["A changed cache center precedes tracking-set difference; each nonempty terrain batch is start, nearest pending full chunks, finish, then feedback; full chunk data installs before its queued light application; unload removes sent chunks and all light; batch finish alone does not mark terrain ready."]
vectors = ["C2-GOLD-CLIENTBOUND-TERRAIN", "C2-BUNDLE-BOUNDARIES", "C2-CACHE-INTEREST", "C2-CHUNK-BATCH-TRACE", "C2-FULL-CHUNK-PALETTES", "C2-CHUNK-HEIGHTMAP-BLOCK-ENTITIES", "C2-LIGHT-MASKS", "C2-BIOME-REFRESH", "C2-CHUNK-UNLOAD", "C2-TERRAIN-READY-TRACE"]
unknowns = []
reproduction = ["Generate simple bodies with the locked Java 25 official codecs; capture a locked-server initial terrain trace; replay every palette, registry, section-count, NBT, light-mask, cache, batch, biome, unload and readiness boundary against the locked client."]
last_commit = "docs(mc-reference): specify C2 terrain stream"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-BLOCK-001"
level = "C2"
state = "play"
direction = "clientbound"
patterns = ["minecraft:block_changed_ack", "minecraft:block_destruction", "minecraft:block_entity_data", "minecraft:block_event", "minecraft:block_update", "minecraft:section_blocks_update"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 4..8/84 use signed ACK, breaker/packed-position/unsigned progress, packed-position/static type/trusted compound, packed-position/two unsigned bytes/static block, packed-position/global state, and packed section/count/VarLong change grammars exactly as specified."]
mappings = ["IDs 8/84 resolve only the locked 32,366-state table, ID 7 the distinct 1,196-block registry, and ID 6 the 49-type block-entity registry; block entities apply only to a matching current typed entity.", "Authoritative states and tags project normalized world snapshots; ACKs release client-retained states; cracks/events remain presentation; all raw IDs, packed coordinates, stages and sequence records stay adapter-local."]
transitions = ["ACK releases every retained latest sequence at or below its value without range/monotonic validation; state deltas stage behind retained predictions or write immediately; block-entity mismatch/cache misses follow explicit ignore paths; malformed mappings fault."]
ordering = ["Use-on and denial corrections can precede next-tick ACK; successful writes can be ACKed before later chunk deltas; changed light precedes block deltas; one/multi-position state packets precede matching block-entity packets; crack/event streams do not acknowledge prediction."]
vectors = ["C2-GOLD-CLIENTBOUND-BLOCK", "C2-BLOCK-CODEC-BOUNDARIES", "C2-BLOCK-SEQUENCE-LIFECYCLE", "C2-BLOCK-PREDICTION-ORDER", "C2-BLOCK-DELTA-MAPPINGS", "C2-BLOCK-ENTITY-DATA", "C2-BLOCK-EVENTS", "C2-BLOCK-DESTRUCTION-PROGRESS", "C2-BLOCK-END-TO-END"]
unknowns = []
reproduction = ["Generate all six goldens with the locked Java 25 codecs and registry bootstrap, then replay ACK/update order, state/type registry boundaries, trusted NBT, cache misses, section duplicates, block events and crack progress against the locked client and server broadcaster."]
last_commit = "docs(mc-reference): specify C2 block convergence"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-SESSION-001"
level = "C2"
state = "play"
direction = "clientbound"
patterns = ["minecraft:disconnect", "minecraft:keep_alive", "minecraft:move_vehicle", "minecraft:ping", "minecraft:player_rotation"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 32/44/57/61/73 use trusted context-free reason NBT, signed-long keepalive, absolute vehicle pose, signed-int ping, and two-boolean relative rotation grammars in the specification."]
mappings = ["Disconnect/liveness are connection-local projections; player rotation and vehicle correction derive from authoritative state but raw IDs, payloads, local vehicle identity and packet structs remain version-adapter state."]
transitions = ["Disconnect closes with the supplied reason; keepalive schedules an exact serverbound ID-28 echo; ping schedules ID-45; player rotation applies immediately and returns ID-32; qualifying vehicle correction snaps only on positional difference and always returns ID-34."]
ordering = ["Keepalive and ping have disjoint echo domains; rotation response follows application without a teleport challenge; vehicle correction applies only to the current locally authoritative root vehicle and its echo enters normal server validation."]
vectors = ["C2-GOLD-CLIENTBOUND-SESSION", "C2-PLAY-DISCONNECT", "C2-PLAY-LIVENESS", "C2-PLAYER-ROTATION", "C2-VEHICLE-CORRECTION-CLIENT", "C2-SESSION-EXCEPTIONAL-FLOATS"]
unknowns = []
reproduction = ["Generate every non-NBT fixture with the locked Java 25 official codec and replay disconnect, frozen-render liveness, relative rotation, irrelevant/current vehicle and exceptional-float branches against the locked client."]
last_commit = "docs(mc-reference): specify C2 movement session"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-ENTITY-SESSION-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:animate", "minecraft:damage_event", "minecraft:hurt_animation", "minecraft:respawn", "minecraft:set_camera", "minecraft:take_item_entity"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 2/25/42/82/93/124 carry entity/action, entity/dynamic damage type/biased causes/optional position, entity/yaw, common spawn/keep mask, camera entity, and source/collector/amount grammars exactly as specified."]
mappings = ["Damage type and respawn dimension type resolve through the frozen configuration registries; all entity numbers are level-local projection keys and damage source position takes precedence over cause/direct lookups.", "Damage, animation, camera, pickup and respawn packets project authoritative outcomes into client presentation/session state; entity IDs, holder raw IDs, masks and packet structs remain adapter-local rather than ECS or persistence identities."]
transitions = ["Unknown entity numbers follow each handler's ignore/fallback rule; valid damage and animation update presentation, camera changes only for a present entity, pickup mutates/removes only a present source, and respawn replaces local player/possibly level while reopening the player-loaded gate."]
ordering = ["Damage event is emitted only on the full unblocked damage-event branch and hurt yaw separately targets the damaged player; set-camera follows server relocation; respawn precedes position correction and the remaining spawn, difficulty, experience, effect, level and permission projections."]
vectors = ["C3-GOLD-CLIENTBOUND-ENTITY-SESSION", "C3-ENTITY-FEEDBACK", "C3-DAMAGE-PROJECTION", "C3-RESPAWN-SESSION", "C3-SPECTATOR-CAMERA", "C3-TAKE-ITEM", "C3-ENTITY-SESSION-END-TO-END"]
unknowns = []
reproduction = ["Generate all six goldens through locked Java 25 codecs with explicit damage/dimension registry snapshots, then replay entity absence/type, action byte, damage-source, keep-mask, camera, pickup amount and respawn-order boundaries."]
last_commit = "docs(mc-reference): specify C3 entity session"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-ENTITY-SPAWN-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:add_entity", "minecraft:remove_entities"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 1/77 carry entity ID, UUID, defaulted static entity-type raw ID, absolute pose, compact motion, three byte rotations and type-specific data; and a signed VarInt count followed by signed entity VarInts exactly as specified."]
mappings = ["The 158-entry static entity-type registry is locked by reports/registries.json; numeric entity and projectile-owner IDs are current-level projection keys, UUID projects normalized authoritative entity/profile identity, and falling-block data resolves only through the 32,366-entry state table.", "Spawn data, packed rotations, raw registry IDs and removal lists remain adapter/session state; Ferrite emits them from namespaced entity types and normalized authoritative relationships without persisting wire numbers."]
transitions = ["Add clears a matching former-vehicle marker before construction, enforces prior player info and client spawn checks, recreates type-specific state before same-ID replacement, then inserts/sounds/marks seen players; removal processes wire order, retains a qualifying player vehicle, discards present targets and drops their debug projections."]
ordering = ["A tracker pairing bundle orders add first, then nondefault metadata, syncable attributes, nonempty equipment, own/vehicle passenger lists and leash; player info must already exist, while unpairing sends canonical one-ID removal after stopSeenByPlayer."]
vectors = ["C3-GOLD-CLIENTBOUND-ENTITY-SPAWN", "C3-ENTITY-SPAWN-CODECS", "C3-ENTITY-TYPE-MAPPING", "C3-ENTITY-CREATION", "C3-ENTITY-SPAWN-DATA", "C3-ENTITY-REMOVAL", "C3-ENTITY-PAIRING-ORDER", "C3-ENTITY-SPAWN-END-TO-END"]
unknowns = []
reproduction = ["Generate both goldens through locked Java 25 codecs with built-in entity type zero, enumerate all recreate/add-packet overrides, then replay registry, player-info, spawn-check, data, replacement/duplicate, vehicle and removal-order boundaries."]
last_commit = "docs(mc-reference): specify C3 entity spawn"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-ENTITY-MOTION-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:entity_position_sync", "minecraft:move_entity_pos", "minecraft:move_entity_pos_rot", "minecraft:move_minecart_along_track", "minecraft:move_entity_rot", "minecraft:rotate_head", "minecraft:set_entity_motion", "minecraft:teleport_entity", "minecraft:projectile_power"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 35/53/54/55/56/83/101/125/135 carry absolute position-motion-rotation, signed-short relative deltas, byte rotations, minecart step lists, compact motion, relative-mask teleport and projectile power exactly as specified."]
mappings = ["Entity IDs and position-codec bases are current-level projection state; relative masks and packed rotations are adapter values, while authoritative position, velocity, rotation, ground state and projectile acceleration remain normalized simulation values.", "Minecart steps are enabled-feature presentation queues and neither their packet records nor interpolation weights enter ECS or persistence state."]
transitions = ["Missing targets are ignored except the retained removed-player-vehicle teleport fallback; local-authoritative entities consume relative base updates without pose/ground application, remote entities snap or interpolate by the documented packet path, and matching projectile/minecart runtime types receive their specialized projections."]
ordering = ["Tracker motion precedes its position/rotation packet, dirty data follows it, head rotation follows dirty data, and hurt-marked self-inclusive motion is last; riding-player teleport applies then returns the documented player/vehicle movement echo only on its fallback/authoritative branches."]
vectors = ["C3-GOLD-CLIENTBOUND-ENTITY-MOTION", "C3-ENTITY-MOTION-CODECS", "C3-RELATIVE-ENTITY-MOVEMENT", "C3-ABSOLUTE-ENTITY-SYNC", "C3-ENTITY-TELEPORT", "C3-MINECART-STEP-QUEUE", "C3-ENTITY-MOTION-PUBLICATION", "C3-ENTITY-MOTION-END-TO-END"]
unknowns = []
reproduction = ["Generate all nine goldens with the locked Java 25 official codecs, then replay missing/local-authoritative/interpolating entities, short-delta endpoints, relative masks, former vehicles, minecart weights and projectile runtime-type boundaries."]
last_commit = "docs(mc-reference): specify C3 entity motion"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-ENTITY-STATE-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:set_entity_data", "minecraft:set_entity_link", "minecraft:set_equipment", "minecraft:set_passengers", "minecraft:update_attributes"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 99/100/102/107/131 carry entity VarInt plus terminated typed metadata entries, two signed ints, continuation-bit equipment entries with item/component patches, a bounded VarInt passenger array, and at most 128 registry-aware attribute snapshots exactly as specified."]
mappings = ["Metadata uses 43 locked serializer IDs and 221 declared class accessors; attributes resolve the 40-entry 26.2 attribute registry, equipment uses eight ordinal slots plus the 1,537-item and 111-component static mappings, and relationship integers remain connection-local entity IDs.", "Ferrite projects namespaced typed authoritative values through those version-locked tables; serializer IDs, slots, raw registry IDs, item-component IDs and client delayed-leash/passenger state remain adapter-local and never enter ECS persistence."]
transitions = ["Metadata applies typed entries in wire order; equipment replaces addressed living slots; attributes replace each present instance's base and complete modifier set; passengers replace a present vehicle's complete direct-passenger list; leash packets install or clear delayed holder resolution for present leashable sources."]
ordering = ["Pairing orders nondefault metadata, all syncable attributes, nonempty equipment, own/vehicle passenger lists and leash after spawn; regular updates order passenger-list change first, pose before dirty metadata/attributes, head after dirty state, while equipment and leash mutations publish on their independent server paths."]
vectors = ["C3-GOLD-CLIENTBOUND-ENTITY-STATE", "C3-ENTITY-METADATA-CODECS", "C3-ENTITY-METADATA-TABLES", "C3-ENTITY-METADATA-APPLICATION", "C3-ENTITY-ATTRIBUTES", "C3-ENTITY-EQUIPMENT", "C3-ENTITY-PASSENGERS", "C3-ENTITY-LEASH", "C3-ENTITY-STATE-PUBLICATION", "C3-ENTITY-STATE-END-TO-END"]
unknowns = []
reproduction = ["Generate all five goldens through locked Java 25 codecs, audit serializer IDs plus every top-level entity accessor/default/class hierarchy, then replay malformed, missing/wrong-type, duplicate, registry, component, modifier, relationship and publication-order boundaries against locked handlers."]
last_commit = "docs(mc-reference): specify C3 entity state"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-ENTITY-EFFECTS-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:explode", "minecraft:remove_mob_effect", "minecraft:update_mob_effect"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 36/78/132 carry center/radius/calculated-block-count/optional knockback/particle/sound/weighted particle recipes; entity/effect removal; and entity/effect/amplifier/duration/flag update in the exact locked order and encodings."]
mappings = ["Effect holders use the strict 40-entry mob-effect map; particle payloads dispatch through the strict 125-entry particle-type map; explosion sounds use a zero/direct or raw-ID-plus-one holder over the 1,968-entry sound-event registry.", "Ferrite projects namespaced authoritative effects, explosion results and per-player knockback through version-local holders; packet IDs, raw registry IDs, flags, calculated particle counts and client visual/blend queues never enter ECS persistence."]
transitions = ["Update resolves a living entity, clamps amplifier to 0..=255, constructs a no-hidden-effect replacement with raw duration/flags, applies blend policy and force-replaces the effect subject to canBeAffected; removal deletes that effect without callbacks; explosion performs sound, primary particle, deferred weighted block particles and optional additive local-player knockback."]
ordering = ["The server completes explosion game event, entity damage, block interaction and fire before sending each player within strict 64-block range; self/passenger effect publication follows any required attribute mutation, active-effect replay precedes passenger-list convergence, and effect metadata/attributes remain separate dirty projections."]
vectors = ["C3-GOLD-CLIENTBOUND-ENTITY-EFFECTS", "C3-EXPLOSION-CODECS", "C3-EXPLOSION-SEMANTICS", "C3-MOB-EFFECT-CODECS", "C3-MOB-EFFECT-APPLICATION", "C3-MOB-EFFECT-PUBLICATION", "C3-ENTITY-EFFECTS-END-TO-END"]
unknowns = []
reproduction = ["Generate all three goldens through locked Java 25 codecs, then replay registry, direct-holder, IEEE, count/weight, radius, range, player-knockback, entity-type, amplifier, duration, flag, blend, passenger and publication-order boundaries against locked handlers."]
last_commit = "docs(mc-reference): specify C3 entity effects"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-CONTAINER-CONVERGENCE-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:container_close", "minecraft:container_set_content", "minecraft:container_set_data", "minecraft:container_set_slot", "minecraft:open_screen", "minecraft:set_cursor_item", "minecraft:set_player_inventory"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 17/18/19/20/59/96/108 carry container VarInt; container/state VarInts, generic optional-stack list and cursor; container VarInt plus signed property/value shorts; container/state VarInts plus signed slot short and stack; container/menu VarInts plus trusted component NBT; stack; and slot VarInt plus stack exactly as specified."]
mappings = ["Open screen resolves the locked 25-entry menu registry; every stack resolves the 1,537-item and 111-component maps, while container/state/property/slot integers remain their separate version-local domains.", "Ferrite projects normalized authoritative menus, slots, cursor, data and inventory through these maps; packet IDs, raw registries, container/state IDs and client GUI bookkeeping never enter ECS persistence."]
transitions = ["Open constructs and selects a typed client menu; exact current IDs apply content/slot/data except container zero targets inventory, cursor has creative-screen suppression, player-inventory uses its list/equipment mapping, and close ignores its ID and closes current state."]
ordering = ["Canonical open sends close/removal when needed, then open, full content/cursor with incremented state, then every data value; delta scans slots and increments state per send, then cursor and data; stale serverbound state forces the full path after authoritative execution."]
vectors = ["C3-GOLD-CLIENTBOUND-CONTAINER", "C3-CONTAINER-EGRESS-CODECS", "C3-CONTAINER-MENU-MAPPING", "C3-CONTAINER-OPEN-CLOSE", "C3-CONTAINER-STATE-LIFECYCLE", "C3-CONTAINER-CLIENT-APPLICATION", "C3-CONTAINER-REMOTE-HASH", "C3-CONTAINER-CONVERGENCE-END-TO-END"]
unknowns = []
reproduction = ["Generate all seven goldens through the locked Java 25 codecs, then replay menu/item/component registries, signed indices, list lengths, state wrap/staleness, creative GUI branches, open/close races and full/delta publication order against locked server synchronizers and client handlers."]
last_commit = "docs(mc-reference): specify C3 container convergence"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-PLAYER-PROJECTION-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:award_stats", "minecraft:cooldown", "minecraft:set_experience", "minecraft:set_health"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 3/22/103/104 carry a registry-dispatched stat-to-signed-VarInt map; strict cooldown-group identifier and signed duration VarInt; progress float then level/total VarInts; and health float, food VarInt, saturation float in exact order."]
mappings = ["Stats dispatch through the nondefaulted nine-entry stat-type map; custom-stat backing IDs throw while block/item/entity-type backing IDs default to air/air/pig; cooldown groups are namespaced identifiers and all vitals are raw signed/IEEE values at codec admission.", "Ferrite projects typed normalized stats, cooldown groups, health/food/saturation and experience while raw registry/packet IDs, dirty/sent markers, client timers and screen callbacks remain adapter-local."]
transitions = ["Health applies local hurt-flash/clamped-health plus direct food/saturation state; experience directly replaces three fields; nonzero cooldown replaces a group and zero removes it; award-stats replaces only included local values and then refreshes an open stats screen."]
ordering = ["Player tick publishes health after food/stats work and experience after scoreboard criteria; cooldown start/expiry publish at their mutation/tick sites; request_stats alone drains dirty stats into exactly one response including empty, with no token or cross-family acknowledgement."]
vectors = ["C3-GOLD-CLIENTBOUND-PLAYER-PROJECTION", "C3-PLAYER-PROJECTION-CODECS", "C3-STAT-MAPPING", "C3-HEALTH-FOOD-PROJECTION", "C3-EXPERIENCE-PROJECTION", "C3-COOLDOWN-PROJECTION", "C3-STATISTICS-DRAIN", "C3-PLAYER-PROJECTION-ORDER", "C3-PLAYER-PROJECTION-END-TO-END"]
unknowns = []
reproduction = ["Generate all four goldens through the locked Java 25 codecs, enumerate every stat dispatch registry, then replay IEEE/signed boundaries, dirty/sent markers, cooldown wrap/expiry, join/relocation/respawn and repeated statistics requests through locked publishers and client handlers."]
last_commit = "docs(mc-reference): specify C3 player projection"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-SPECIAL-SCREENS-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:mount_screen_open", "minecraft:open_book", "minecraft:open_sign_editor"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 41/58/60 carry signed container/column VarInts plus fixed signed entity int; strict interaction-hand ordinal VarInt; and packed signed block position plus front/back boolean, respectively."]
mappings = ["Mount activation resolves an already tracked horse/nautilus and reuses container convergence; book activation resolves the handler-time hand stack's written/writable components; sign activation resolves the current sign block entity and ordinary/hanging editor subtype.", "Ferrite projects normalized mount menus, resolved book components and authorized sign sides while entity/container IDs, hand ordinals, GUI state and signed allocation arithmetic remain version-local."]
transitions = ["Mount allocates columns-times-three before its entity type gate then selects a specialized current menu/screen; book opens a view only for a recognized current component; sign opens only for a present sign block entity and later closes through the separately specified sign-update request."]
ordering = ["Canonical mount open closes the old menu then sends ID 41 before ordinary full container synchronization; resolved book stack changes broadcast before ID 58; sign open stores editor authorization then sends ID 8 block correction before ID 60, with earlier block-entity state supplying text."]
vectors = ["C3-GOLD-CLIENTBOUND-SPECIAL-SCREENS", "C3-SPECIAL-SCREEN-CODECS", "C3-MOUNT-SCREEN-ACTIVATION", "C3-BOOK-VIEW-ACTIVATION", "C3-SIGN-EDITOR-ACTIVATION", "C3-SPECIAL-SCREENS-ORDER", "C3-SPECIAL-SCREENS-END-TO-END"]
unknowns = []
reproduction = ["Generate all three goldens through the locked Java 25 codecs, then replay signed allocation/registry timing, missing/wrong tracked state, hand mutation, book component resolution, sign state correction and editor activation/submission against locked server/client handlers."]
last_commit = "docs(mc-reference): specify C3 special screens"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-RECIPE-BOOK-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:place_ghost_recipe", "minecraft:recipe_book_remove"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 63 carries a signed container VarInt followed by a registry-aware dispatched RecipeDisplay; ID 75 carries a generic VarInt-counted list of signed RecipeDisplayId VarInts."]
mappings = ["Ghost payloads dispatch through the locked five-entry recipe-display-type map and all nested slot/item/component registries; removal IDs index the session-local feature-filtered RecipeManager display list rather than any registry or resource-key domain.", "Ferrite projects normalized display payloads and parent-recipe publication/removal while packet/container/display raw IDs, local highlights, search collections and ghost GUI slots remain adapter-local."]
transitions = ["A matching current container plus RecipeUpdateListener screen replaces its ghost slots from the supplied display; removal deletes each exact known/highlight display ID in wire order and refreshes collections/search/open recipe UI exactly once, including for an empty list."]
ordering = ["The server sends ghost immediately only after an admitted placement cannot craft and clears the grid, before ordinary later container deltas; recipe removal follows authoritative parent-recipe removal and has no acknowledgement or generation token."]
vectors = ["C3-GOLD-CLIENTBOUND-RECIPE-BOOK", "C3-RECIPE-BOOK-CODECS", "C3-RECIPE-DISPLAY-ID-MAPPING", "C3-GHOST-RECIPE-APPLICATION", "C3-RECIPE-BOOK-REMOVE", "C3-RECIPE-BOOK-HIGHLIGHTS", "C3-RECIPE-BOOK-ORDER", "C3-RECIPE-BOOK-END-TO-END"]
unknowns = []
reproduction = ["Generate both goldens through the locked Java 25 codecs, then replay every display dispatch/registry mapping, signed IDs, list count/malformed forms, current-menu/screen races, duplicate/missing removals and client refresh behavior against the locked publishers and handlers."]
last_commit = "docs(mc-reference): specify C3 recipe book"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-MERCHANT-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:merchant_offers"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 52 carries signed container ID, generic VarInt-counted MerchantOffer list, signed villager level and XP VarInts, show-progress boolean and can-restock boolean; each offer carries exact-component ItemCost A, nonempty result stack, optional ItemCost B, derived out-of-stock boolean, four signed ints, raw float multiplier and signed demand int in locked order."]
mappings = ["Strict item/component raw IDs resolve through the locked 1,537-item and 111-component maps; ItemCost predicates require the named item and equality for every listed component but allow extra stack components, while the first cost's payable count applies Java wrapping/floor/clamp arithmetic and the second cost remains its base count.", "Ferrite projects copied normalized offers, XP/level and UI flags only to the matching current merchant menu; raw registries, container ID, presentation stacks, offer ordering, GUI selection and derived price caches remain adapter-local."]
transitions = ["Decode fixes network reward-experience true, canonicalizes a true out-of-stock flag by replacing uses with maxUses, then installs special price; the client silently ignores wrong/nonmerchant menus and otherwise sequentially replaces its copied offer list, XP, level and flags without requiring a screen."]
ordering = ["Canonical opening sends generic ID 59 and initial ID 18/19 convergence before ID 52 and omits ID 52 when there are no offers; later selection is local-first ID 51 and ordinary container traffic converges payment/result state, with no merchant acknowledgement or generation token."]
vectors = ["C3-GOLD-CLIENTBOUND-MERCHANT", "C3-MERCHANT-CODECS", "C3-MERCHANT-COST-MAPPING", "C3-MERCHANT-OFFER-DECODE", "C3-MERCHANT-OFFER-PRICING", "C3-MERCHANT-CLIENT-APPLICATION", "C3-MERCHANT-ORDER", "C3-MERCHANT-END-TO-END"]
unknowns = []
reproduction = ["Generate the golden through the locked Java 25 registry-aware codec, then replay list/item/component/stack boundaries, raw numeric and out-of-stock normalization, exact-component matching and price arithmetic, menu races, canonical open ordering and selection/container convergence against locked publishers and handlers."]
last_commit = "docs(mc-reference): specify C3 merchant trading"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-INVENTORY-PROGRESSION-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:map_item_data", "minecraft:tag_query", "minecraft:update_advancements"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 51 carries signed map ID, scale byte, locked boolean, optional generic decoration list and width-zero-sentinel color patch; ID 123 carries signed transaction VarInt plus quota/depth-bounded nullable root compound NBT; ID 130 carries reset boolean, generic added/removed/progress collections and show-advancements boolean with the exact reduced advancement/display/progress grammar."]
mappings = ["Map IDs normalize to durable map saved-data keys while decoration types, item templates and components resolve through locked registries; tag transactions are ephemeral callback correlation, and advancement identifiers/criterion names normalize to namespaced advancement identity and requirement-local strings rather than collection order or raw IDs.", "Ferrite projects map pixels/decorations, debug query response data and advancement definitions/progress; packet transactions, registry raw IDs, map texture objects, client callbacks, GUI trees, toast state and telemetry remain adapter-local."]
transitions = ["Map handling creates missing client map data once, replaces decorations when present, applies color bytes nontransactionally, then refreshes the texture; tag responses complete only the exact latest nonnull callback; advancement handling resets optionally, recursively removes, dependency-adds, normalizes progress and conditionally reports telemetry/toasts in exact order."]
ordering = ["Canonical maps consume dirty pixels immediately and publish dirty decorations when the per-holder tick++ counter's old value is zero modulo five (including the first dirty opportunity); tag queries echo the sole latest transaction only after permission/object resolution; advancement flush publishes visibility add/remove and visible dirty progress as a tokenless delta, with reset suppressing initial telemetry/toasts."]
vectors = ["C3-GOLD-CLIENTBOUND-INVENTORY-PROGRESSION", "C3-MAP-PROJECTION-CODECS", "C3-MAP-PATCH-APPLICATION", "C3-MAP-PUBLICATION-ORDER", "C3-TAG-QUERY-CODECS", "C3-TAG-QUERY-CORRELATION", "C3-ADVANCEMENT-CODECS", "C3-ADVANCEMENT-TREE-APPLICATION", "C3-ADVANCEMENT-PROGRESS-PRESENTATION", "C3-INVENTORY-PROGRESSION-ORDER", "C3-INVENTORY-PROGRESSION-END-TO-END"]
unknowns = []
reproduction = ["Generate all three goldens with the locked Java 25 official codecs, then replay map decoration/patch malformed forms and dirty cadence, NBT quota/depth/root/correlation branches, and advancement collection/parent/removal/progress/presentation permutations against the locked publishers and client handlers."]
last_commit = "docs(mc-reference): specify C3 clientbound inventory progression"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-TITLE-TAB-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:clear_titles", "minecraft:select_advancements_tab", "minecraft:set_action_bar_text", "minecraft:set_subtitle_text", "minecraft:set_title_text", "minecraft:set_titles_animation", "minecraft:tab_list"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 87/112/114 carry one trusted registry-aware component, ID 122 two; ID 14 carries one boolean, ID 85 a nullable default-bounded identifier, and ID 115 three big-endian signed timing ints."]
mappings = ["Title/action-bar/tab components retain trusted component semantics; advancement identifiers resolve against the handler-time client tree, while empty flattened header/footer strings normalize to null.", "Ferrite projects normalized presentation and namespaced advancement selection intent while packet IDs, client counters, holder/widget objects and absent acknowledgements remain adapter/client-local."]
transitions = ["Action bar replaces and restarts 60 ticks; subtitle is passive; title restarts from current phase sum; nonnegative timing fields selectively replace and restart only an active title; clear always removes title/subtitle/time and optionally resets 10/70/20 defaults.", "Server advancement selection sanitizes to displayed roots and sends only identity changes; the client performs lookup, selects null for missing IDs, notifies only on identity change and never echoes the correction."]
ordering = ["Title commands target selected players directly, reuse clear/time packets but resolve text per player in iteration order; all seven packets apply independent receive-order fields with no sequence, generation, retry or acknowledgement."]
vectors = ["C3-GOLD-CLIENTBOUND-TITLE-TAB", "C3-TITLE-TAB-CODECS", "C3-TITLE-TIMER-STATE", "C3-ACTION-BAR-REPLACEMENT", "C3-ADVANCEMENT-TAB-CORRECTION", "C3-TAB-LIST-DECORATION", "C3-TITLE-PUBLICATION", "C3-TITLE-TAB-ORDER", "C3-TITLE-TAB-END-TO-END"]
unknowns = []
reproduction = ["Generate all seven goldens through locked Java 25 codecs, then replay signed timer/clear/action-bar state, tab lookup/listener behavior, empty header/footer normalization, target-specific command resolution failures and packet reordering against the locked handlers and publishers."]
last_commit = "docs(mc-reference): specify C3 title and tab presentation"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-COMBAT-LOOK-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:player_combat_end", "minecraft:player_combat_enter", "minecraft:player_combat_kill", "minecraft:player_look_at"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 66/67 carry signed duration VarInt or no body; ID 68 carries signed player VarInt plus trusted death component; ID 71 carries strict from-anchor, three doubles, boolean and conditional signed entity VarInt/to-anchor."]
mappings = ["Combat-kill IDs resolve only to the exact handler-time local-player object; entity look-at prefers the handler-time feet/eyes anchor and otherwise uses send-time coordinate fallback, with exact atan2/wrap/float rotation math.", "Ferrite projects normalized death/look intent and authoritative rotation while raw connection entity IDs, client screens, fallback coordinates, prior rotations and packet lifecycle order stay adapter/client-local."]
transitions = ["Combat enter/end are intentional client no-ops; a qualifying kill opens the current-hardcore death screen or immediately sends PERFORM_RESPAWN and resets toggle keys according to the login-derived screen flag; look-at imperatively replaces current/previous pitch/yaw/head/body rotations."]
ordering = ["Server combat lifecycle and death packets target only the player connection, with death before later cleanup and a 256-character exceptional-send fallback; server look-at rotates authority before direct send and has no tracking/dimension/range gate."]
vectors = ["C3-GOLD-CLIENTBOUND-COMBAT-LOOK", "C3-COMBAT-LOOK-CODECS", "C3-COMBAT-LIFECYCLE-NOOP", "C3-COMBAT-KILL-IDENTITY", "C3-LOOK-AT-RESOLUTION", "C3-COMBAT-DEATH-PUBLICATION", "C3-LOOK-AT-PUBLICATION", "C3-COMBAT-LOOK-ORDER", "C3-COMBAT-LOOK-END-TO-END"]
unknowns = []
reproduction = ["Generate all four goldens through the locked Java 25 codecs, then replay enter/end no-ops, kill identity/screen/respawn branches, death fallback publication, both anchor forms, missing/moving targets, nonfinite rotation math and ordering against locked publishers and handlers."]
last_commit = "docs(mc-reference): specify C3 combat and look projection"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-BOSS-WAYPOINT-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:boss_event", "minecraft:waypoint"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 9 carries UUID plus a strict six-way boss operation with operation-specific trusted component, float, strict style and property-byte fields; ID 138 carries a modulo-three operation plus a complete UUID/string-keyed waypoint with unregistered style identifier, optional RGB and strict empty/position/chunk/azimuth content."]
mappings = ["Boss UUIDs and waypoint Either identifiers key client presentation collections; boss styles/properties map fixed ordinals/bits, while waypoint position/chunk/angle data, optional opaque color and style resource identity retain their locked semantics without a style registry lookup.", "Ferrite projects normalized boss and locator intent plus authoritative UUID/entity state; operation ordinals, string wrappers, client interpolation anchors, icons, collection objects and renderer ordering remain adapter/client-local."]
transitions = ["Boss add/remove/update replace or mutate a linked map with 100-ms progress interpolation and any-bar aggregate properties; waypoint track replaces complete state, untrack removes by key and update mutates only matching location content, never icon/type, with missing targets failing and mismatches warning."]
ordering = ["Boss publishers suppress equal visible deltas and snapshot on visibility/add transitions; waypoint publishers select block/chunk/azimuth connections by strict admission/view/distance thresholds and remake representation/icon changes with track under the same UUID, all without generation, acknowledgement or stable set/map order."]
vectors = ["C3-GOLD-CLIENTBOUND-BOSS-WAYPOINT", "C3-BOSS-WAYPOINT-CODECS", "C3-BOSS-COLLECTION", "C3-BOSS-PUBLICATION", "C3-WAYPOINT-OPERATIONS", "C3-WAYPOINT-PROJECTION", "C3-WAYPOINT-PUBLICATION", "C3-BOSS-WAYPOINT-ORDER", "C3-BOSS-WAYPOINT-END-TO-END"]
unknowns = []
reproduction = ["Generate all six goldens through locked Java 25 official codecs, then replay strict/modulo enum boundaries, boss collection/interpolation/visibility, waypoint key/type/icon replacement, projection, exact connection thresholds, gamerule/team remakes and packet reordering against locked publishers and handlers."]
last_commit = "docs(mc-reference): specify C3 boss and waypoint projection"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-CHAT-SCORE-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:command_suggestions", "minecraft:custom_chat_completions", "minecraft:delete_chat", "minecraft:disguised_chat", "minecraft:player_chat", "minecraft:player_info_remove", "minecraft:reset_score", "minecraft:set_display_objective", "minecraft:set_objective", "minecraft:set_player_team", "minecraft:set_score", "minecraft:system_chat"]
status = "Todo"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify chat/signature presentation, commands, player list and scoreboard projections."]
reproduction = ["Trace locked server broadcasters and client presentation handlers, including ordering and malformed component/signature behavior."]
last_commit = ""

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-WORLD-EFFECT-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:level_event"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 46 carries big-endian signed event int, packed block-position long, big-endian signed data int and global boolean; all complete numeric/boolean bit patterns are transport-valid."]
mappings = ["The false table defines all 80 locked local IDs and the true table only global IDs 1023/1028/1038; event-owned data maps through the configured jukebox-song registry, 32,366-state global block table, fixed item/sound/particle identities, direction/axis/face encodings and exact signed formulas.", "Ferrite projects normalized namespaced effect intent and semantic data while packet/event integers, dynamic raw IDs, client block/entity caches, RNG, HUD, sounds and particles remain adapter/client-local."]
transitions = ["Local dispatch applies sound, jukebox, particle and current-state effects in source order, including documented fallback/no-op and partial-failure prefixes; global dispatch requires an initialized camera and places its recognized sound two blocks toward the carried position."]
ordering = ["Canonical local publication excludes an exact source player and uses same-dimension strict radius 64; global publication follows globalSoundEvents, either falling back local or reaching every player with per-player actual/projected/self positions; receive order is tokenless and unacknowledged."]
vectors = ["C3-GOLD-CLIENTBOUND-LEVEL-EVENT", "C3-LEVEL-EVENT-CODECS", "C3-LEVEL-EVENT-DISPATCH", "C3-LEVEL-EVENT-DATA-MAPPINGS", "C3-LEVEL-EVENT-RANDOM-ORDER", "C3-LEVEL-EVENT-JUKEBOX-LIFETIME", "C3-LEVEL-EVENT-PUBLICATION", "C3-LEVEL-EVENT-ORDER", "C3-LEVEL-EVENT-END-TO-END"]
unknowns = []
reproduction = ["Generate both goldens through the locked Java 25 codec, then replay all 80 local and three global-only IDs, wrong-table/unknown IDs, every data mapping and helper random/failure branch, jukebox lifetime, and exact local/global publisher audiences against the locked broadcasters and handlers."]
last_commit = "docs(mc-reference): specify C3 level-event projection"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-PARTICLE-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:level_particles"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 47 carries override-limiter and always-show booleans, X/Y/Z doubles, X/Y/Z spread floats, max-speed float, signed count int, then strict particle-type raw ID and that type's options payload."]
mappings = ["All 125 locked particle types and option codecs resolve through the configured particle registry; the packet and 32 type-owned override flags remain presentation policy, while raw registry IDs, client random streams and engine particles remain adapter/client-local."]
transitions = ["Count zero attempts one exact-position particle with component velocity maxSpeed*spread; positive count attempts count Gaussian position/velocity samples and negative count attempts none; type/packet override bypasses distance/settings while always-show only changes reduced/minimal sampling."]
ordering = ["Canonical publication sends to same-level players whose block-position centers are strictly within 32 blocks, or 512 when packet override is true; client attempts run immediately in receive order, consume documented RNG before limiter decisions and have no response, generation or acknowledgement."]
vectors = ["C3-GOLD-CB-LEVEL-PARTICLES", "C3-PARTICLE-CODECS", "C3-PARTICLE-COUNT-SAMPLING", "C3-PARTICLE-LIMITER-GATES", "C3-PARTICLE-PUBLICATION", "C3-PARTICLE-FAULT-ORDER", "C3-PARTICLE-END-TO-END"]
unknowns = []
reproduction = ["Generate the false/zero/simple-type-raw-ID-zero golden with the locked Java 25 codec, re-use the audited 125-type option matrix, and replay signed counts, Gaussian draws, float narrowing, 32 override types, user settings, exact distance audiences, creation failures and interleaving against the locked server/client handlers."]
last_commit = "docs(mc-reference): specify C3 particle projection"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-SOUND-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:sound_entity", "minecraft:sound", "minecraft:stop_sound"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 117/116 carry registered-or-direct sound holder, strict sound-source enum, fixed-point position ints or entity VarInt, volume/pitch floats and seed long; ID 119 carries a low-two-bit mask followed conditionally by source enum then sound identifier."]
mappings = ["Registered sound holders resolve through the locked 1,968-entry sound-event registry while direct holders retain namespaced location and optional range; source ordinals map only to the eleven locked mixer categories, entity IDs remain connection-local and stop names target resolved sound-instance identifiers."]
transitions = ["Positional sound constructs one immediate seeded linear instance at int-to-float-to-double eighth-block coordinates; entity sound ignores a missing entity or creates a seeded entity-bound instance that follows float-rounded position and stops on removal; stop applies the four source/name filter combinations to current sound-engine instances."]
ordering = ["Canonical positional/entity publishers exclude an optional source player and send within strict squared sound range in the same dimension; stop commands target selected players directly; spawn-before-entity-sound and receive order matter, with no response, generation or acknowledgement."]
vectors = ["C3-GOLD-CLIENTBOUND-SOUND", "C3-SOUND-CODECS", "C3-POSITIONAL-SOUND-APPLICATION", "C3-ENTITY-SOUND-APPLICATION", "C3-SOUND-PUBLICATION", "C3-STOP-SOUND-FILTERS", "C3-SOUND-ORDER", "C3-SOUND-END-TO-END"]
unknowns = []
reproduction = ["Generate registered-raw-ID-zero positional/entity and unfiltered-stop goldens with the locked Java 25 official codecs, then replay direct/registered holders, source ordinals, fixed-point/IEEE boundaries, entity lifetime/order, strict range/exclusion, seeded resource selection and all stop masks against the locked publishers and client sound engine."]
last_commit = "docs(mc-reference): specify C3 sound projection"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-WORLD-BORDER-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:set_border_center", "minecraft:set_border_lerp_size", "minecraft:set_border_size", "minecraft:set_border_warning_delay", "minecraft:set_border_warning_distance"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 88/89/90 carry center X/Z doubles, old/new size doubles plus signed VarLong duration, and one size double; IDs 91/92 carry signed VarInt warning time and distance respectively."]
mappings = ["All five packets project the current dimension's normalized world-border geometry or warning configuration; IEEE payloads, VarInt/VarLong encodings, packet IDs, listener instances and client game-time anchors remain version-adapter or connection-local state."]
transitions = ["Center updates geometry without replacing size motion; immediate size replaces any motion with a static extent; lerp replaces the extent using packet old/new values, raw duration and receive-time client game time; warning time and distance replace independent signed fields."]
ordering = ["Each authoritative mutator synchronously emits one dimension-scoped delta even for an equal value, while tick progress, damage and safe-zone changes emit none; deltas have no sequence or acknowledgement and apply in receive order, with ID 43 snapshots independently replacing the complete border on join or level transition."]
vectors = ["C3-GOLD-CLIENTBOUND-WORLD-BORDER", "C3-WORLD-BORDER-DELTA-CODECS", "C3-WORLD-BORDER-DELTA-APPLICATION", "C3-WORLD-BORDER-DELTA-PUBLICATION", "C3-WORLD-BORDER-DELTA-ORDER", "C3-WORLD-BORDER-DELTA-END-TO-END"]
unknowns = []
reproduction = ["Generate all five zero-valued goldens with the locked Java 25 official codecs, then replay IEEE endpoints, signed integer encodings, equal/unequal and nonpositive-duration lerps, repeated authoritative mutations, dimension audiences, delayed deltas and ID-43 replacement against the locked border listener and client handlers."]
last_commit = "docs(mc-reference): specify C3 world-border deltas"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-OPTIONAL-001"
level = "C4"
state = "play"
direction = "clientbound"
patterns = ["minecraft:cookie_request", "minecraft:custom_payload", "minecraft:debug/block_value", "minecraft:debug/chunk_value", "minecraft:debug/entity_value", "minecraft:debug/event", "minecraft:debug_sample", "minecraft:game_rule_values", "minecraft:game_test_highlight_pos", "minecraft:low_disk_space_warning", "minecraft:pong_response", "minecraft:resource_pack_pop", "minecraft:resource_pack_push", "minecraft:start_configuration", "minecraft:store_cookie", "minecraft:test_instance_block_status", "minecraft:transfer", "minecraft:update_tags", "minecraft:custom_report_details", "minecraft:server_links", "minecraft:clear_dialog", "minecraft:show_dialog"]
status = "Todo"
responsibility = "Optional"
owner = "protocol/play-clientbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify reconfiguration, cookies, payload/resource/transfer/dialog/report gates, diagnostics, debug/test projections, gamerule inspection, ticking step and low-disk presentation."]
reproduction = ["Trace locked common/game sends and client handlers for every optional packet, request correlation and feature/permission gate."]
last_commit = ""
