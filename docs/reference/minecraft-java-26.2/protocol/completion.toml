version = "26.2"

[inventory]
expected_count = 256
entries_sha1 = "f34b0956b6399c749d4638cd6d3c9226685f41fa"

# This is the independent protocol work queue. Selectors must partition the locked packet report
# exactly once. Broad Todo families are recoverable placeholders and must be split before they can
# become Specified, GatedOptional, NonServerResponsibility, or SourceInconclusive.

[[family]]
id = "PROTO-HANDSHAKE-SERVERBOUND-001"
level = "C0"
state = "handshake"
direction = "serverbound"
patterns = ["*"]
status = "Specified"
responsibility = "Required"
owner = "protocol/handshake-and-status"
specification = "handshake-and-status.md"
evidence = ["OFF-META-001", "OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 0; protocol version signed VarInt, host UTF(255), unsigned big-endian port, intent VarInt 1=status/2=login/3=transfer."]
mappings = ["Status selects a connection-local discovery session; host/port remain untrusted routing context and no C0 field enters simulation or persistence.", "No registry, entity-metadata, data-component, or acknowledgement mapping occurs in handshake."]
transitions = ["Terminal intention switches to status without version comparison, to exact-version login, or to transfer-gated login; invalid intent and unavailable status close."]
ordering = ["The intention is the only handshake packet and must be a complete frame before any packet is decoded under the selected state."]
vectors = ["C0-GOLD-INTENTION-STATUS", "C0-HOST-BOUNDARY", "C0-HOST-MULTIBYTE", "C0-HOST-MALFORMED-UTF8", "C0-PORT-BOUNDARY", "C0-INTENT-ILLEGAL", "C0-STATUS-PROTOCOL-OPAQUE", "C0-STATUS-DISABLED", "C0-TRANSFER-GATE"]
unknowns = []
reproduction = ["Regenerate OFF-REPORT-001, inspect the locked intention codec and server/client transition handlers, then run the named generated-boundary and raw-frame vectors in protocol/conformance.md."]
last_commit = "docs(mc-reference): specify C0 handshake and status"

[[family]]
id = "PROTO-STATUS-SERVERBOUND-001"
level = "C0"
state = "status"
direction = "serverbound"
patterns = ["*"]
status = "Specified"
responsibility = "Required"
owner = "protocol/handshake-and-status"
specification = "handshake-and-status.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 0 status request has no fields; ID 1 ping request has one signed big-endian 64-bit opaque token."]
mappings = ["Status request reads the cached normalized status snapshot without gameplay mutation; ping maps its opaque 64 bits directly to pong.", "No wire registry or metadata mapping occurs in status serverbound."]
transitions = ["First status request marks requested and sends one response; duplicate request closes; ping is legal before or after request, sends one exact echo, then closes."]
ordering = ["Vanilla order is request then response then ping; the server does not require request before ping and has no separate acknowledgement number."]
vectors = ["C0-GOLD-STATUS-REQUEST", "C0-GOLD-PING", "C0-FRAME-PARTIAL", "C0-ID-UNKNOWN", "C0-PACKET-TRAILING", "C0-PING-TRUNCATED", "C0-STATUS-DUPLICATE", "C0-PING-BEFORE-STATUS", "C0-PING-SIGNED-BOUNDARIES", "C0-HAPPY-TRACE"]
unknowns = []
reproduction = ["Regenerate OFF-REPORT-001, inspect the locked status codecs/server listener, and execute the named raw-frame and lifecycle vectors in protocol/conformance.md."]
last_commit = "docs(mc-reference): specify C0 handshake and status"

[[family]]
id = "PROTO-STATUS-CLIENTBOUND-001"
level = "C0"
state = "status"
direction = "clientbound"
patterns = ["*"]
status = "Specified"
responsibility = "Required"
owner = "protocol/handshake-and-status"
specification = "handshake-and-status.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 0 response is UTF(32767) compact JSON with description, optional players/version/favicon and default-false enforcesSecureChat; ID 1 pong is one signed big-endian 64-bit token."]
mappings = ["Normalized status fields project through the locked component, UUID and favicon mappings; pong is an exact token echo.", "C0 has no registry, entity metadata, or data-component numeric mappings."]
transitions = ["Response leaves server status open for ping; pong is sent before server closure. Vanilla client sends ping after its first response, rejects a second response, and closes after pong."]
ordering = ["Required happy order is response before vanilla ping and pong before closure; server echo correlation is bit-exact even though vanilla latency uses its local start time."]
vectors = ["C0-GOLD-STATUS-MINIMAL", "C0-GOLD-STATUS-POPULATED", "C0-GOLD-PONG", "C0-JSON-MINIMAL", "C0-JSON-LENIENT-OPTIONAL", "C0-JSON-ROOT", "C0-JSON-LENGTH", "C0-FAVICON-PRESENTATION", "C0-HAPPY-TRACE"]
unknowns = []
reproduction = ["Generate golden bytes through the locked official response codec, inspect the locked client status listener and icon validator, then execute the named decode and end-to-end vectors in protocol/conformance.md."]
last_commit = "docs(mc-reference): specify C0 handshake and status"

[[family]]
id = "PROTO-LOGIN-SERVERBOUND-REQUIRED-001"
level = "C1"
state = "login"
direction = "serverbound"
patterns = ["minecraft:hello", "minecraft:login_acknowledged"]
status = "Specified"
responsibility = "Required"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 0 hello is UTF(16) name then 16-byte supplied UUID; ID 3 acknowledgement is fieldless and terminal."]
mappings = ["Offline policy ignores supplied UUID and derives UUIDv3 from UTF-8 OfflinePlayer:<exact name>; acknowledgement commits the connection-local configuration transition.", "No login field is a gameplay registry, metadata, or data-component numeric mapping."]
transitions = ["HELLO accepts one valid name then VERIFYING/admission/compression/PROTOCOL_SWITCHING; acknowledgement is legal only in PROTOCOL_SWITCHING and installs both configuration directions."]
ordering = ["Hello follows exact-version login intention; acknowledgement follows terminal login finished and precedes the vanilla brand/client-information configuration packets."]
vectors = ["C1-GOLD-LOGIN-HELLO", "C1-GOLD-LOGIN-ACK-RAW", "C1-GOLD-LOGIN-ACK-C256", "C1-LOGIN-OFFLINE-TRACE", "C1-NAME-BOUNDARIES", "C1-SUPPLIED-UUID-IGNORED", "C1-OFFLINE-UUID-CASE", "C1-HELLO-DUPLICATE", "C1-ACK-EARLY-DUPLICATE", "C1-LOGIN-TIMEOUT"]
unknowns = []
reproduction = ["Encode independent goldens through locked login codecs, inspect server/client login state handlers, and run the named offline identity, illegal-order, timeout, and terminal-transition vectors."]
last_commit = "docs(mc-reference): specify C1 login protocol"

[[family]]
id = "PROTO-LOGIN-SERVERBOUND-OPTIONAL-001"
level = "C4"
state = "login"
direction = "serverbound"
patterns = ["minecraft:key", "minecraft:custom_query_answer", "minecraft:cookie_response"]
status = "GatedOptional"
responsibility = "Optional"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 1 key has two VarInt-length byte arrays; ID 2 custom answer has transaction VarInt plus <=1,048,576-byte discarded remainder; ID 4 cookie response has identifier plus nullable <=5,120-byte array."]
mappings = ["Key maps only inside the online authentication gate; custom query/cookie correlate connection-local transaction/key state and never gameplay state."]
transitions = ["Key is legal only in KEY and enables encryption/authentication; base listener disconnects every custom-query answer and cookie response as unexpected."]
ordering = ["Key follows encryption hello; query/cookie answers require an explicitly owned request, which the base listener never sends."]
vectors = ["C1-ONLINE-GATE", "C1-CUSTOM-QUERY-GATE", "C1-COOKIE-GATE"]
unknowns = []
reproduction = ["Run deterministic RSA/challenge/session-service fixtures and inject bounded query/cookie packets into locked client and base-server listeners as defined in protocol/conformance.md."]
last_commit = "docs(mc-reference): specify C1 login protocol"

[[family]]
id = "PROTO-LOGIN-CLIENTBOUND-REQUIRED-001"
level = "C1"
state = "login"
direction = "clientbound"
patterns = ["minecraft:login_disconnect", "minecraft:login_finished", "minecraft:login_compression"]
status = "Specified"
responsibility = "Required"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 0 disconnect is component JSON UTF(262144); ID 2 terminal finished is bounded game profile plus session UUID; ID 3 compression is threshold VarInt."]
mappings = ["Disconnect projects a connection failure component; finished projects normalized identity and server-session UUID; compression changes only transport state.", "Profile properties use bounded name/value/nullable-signature mapping; login has no gameplay registry mapping."]
transitions = ["Admission failure sends disconnect then closes; nonnegative threshold sends compression before enabling its envelope; finished enters client configuration receive state before acknowledgement."]
ordering = ["Compression negotiation itself is uncompressed and its send callback precedes finished; finished is terminal and acknowledgement uses the matching post-negotiation envelope."]
vectors = ["C1-GOLD-COMPRESSION-256", "C1-GOLD-LOGIN-FINISHED-RAW", "C1-GOLD-LOGIN-FINISHED-C256", "C1-LOGIN-OFFLINE-TRACE", "C1-ADMISSION-DENIED", "C1-COMPRESSION-DISABLED", "C1-COMPRESSION-ZERO", "C1-COMPRESSION-BOUNDARY", "C1-COMPRESSION-DECLARATION", "C1-PROFILE-PROPERTIES", "C1-SESSION-ID"]
unknowns = []
reproduction = ["Generate locked-codec profile/threshold goldens and execute admission, compression, property-bound, session-ID, and terminal-order vectors from protocol/conformance.md."]
last_commit = "docs(mc-reference): specify C1 login protocol"

[[family]]
id = "PROTO-LOGIN-CLIENTBOUND-OPTIONAL-001"
level = "C4"
state = "login"
direction = "clientbound"
patterns = ["minecraft:hello", "minecraft:custom_query", "minecraft:cookie_request"]
status = "GatedOptional"
responsibility = "Optional"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 1 encryption hello has UTF(20) server ID, key/challenge byte arrays and authenticate boolean; ID 4 query has transaction VarInt, identifier and <=1,048,576-byte raw remainder; ID 5 cookie request has identifier."]
mappings = ["Encryption hello selects online authentication; query/cookie requests map only to explicitly owned connection-local extension/transfer state."]
transitions = ["Authentication-enabled server sends hello and enters KEY; base listener never sends query/cookie requests, while vanilla client would answer them with correlated null/nullable values."]
ordering = ["Encryption hello follows login hello and precedes key; query/cookie requests require a pending request before their correlated serverbound answer."]
vectors = ["C1-ONLINE-GATE", "C1-CUSTOM-QUERY-GATE", "C1-COOKIE-GATE"]
unknowns = []
reproduction = ["Drive the locked vanilla client with deterministic encryption, custom-query, and cookie request fixtures, then verify the base server's closed gates."]
last_commit = "docs(mc-reference): specify C1 login protocol"

[[family]]
id = "PROTO-CONFIGURATION-SERVERBOUND-REQUIRED-001"
level = "C1"
state = "configuration"
direction = "serverbound"
patterns = ["minecraft:client_information", "minecraft:custom_payload", "minecraft:finish_configuration", "minecraft:keep_alive", "minecraft:pong", "minecraft:select_known_packs"]
status = "Specified"
responsibility = "Required"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 0/2/3/4/5/7 are bounded client information, channel plus payload, terminal unit, signed long, signed int, and <=64 known-pack list respectively."]
mappings = ["Client information becomes the latest connection cookie; brand is ignored by the base server; known packs select only registry data elision; liveness tokens and finish remain connection-local.", "Registry/tag acknowledgement and play transition never expose packet structs or numeric IDs to world persistence."]
transitions = ["Known-pack selection is legal only in synchronize_registries; exact pending keepalive echo clears the challenge; finish is legal only in join_world and installs play after registry/spawn readiness."]
ordering = ["Brand/client information may interleave with server start; known-pack response precedes registry/tag completion; terminal finish follows server finish and precedes all serverbound play packets."]
vectors = ["C1-GOLD-CONFIG-CLIENT-INFO", "C1-GOLD-CONFIG-BRAND-SB", "C1-GOLD-CONFIG-KNOWN-RESPONSE", "C1-GOLD-CONFIG-FINISH", "C1-GOLD-CONFIG-KEEPALIVE", "C1-GOLD-CONFIG-PONG", "C1-CONFIG-HAPPY-TRACE", "C1-CONFIG-KNOWN-ORDER", "C1-CONFIG-FINISH-ORDER", "C1-CONFIG-KEEPALIVE-STATE", "C1-CONFIG-CLIENT-INFO-BOUNDARIES", "C1-CONFIG-CUSTOM-PAYLOAD-BOUNDS"]
unknowns = []
reproduction = ["Generate official-codec frames and replay the minimum offline task queue, known-pack equality branches, client-information bounds, liveness challenge states, and terminal transition vectors in protocol/conformance.md."]
last_commit = "docs(mc-reference): specify C1 configuration protocol"

[[family]]
id = "PROTO-CONFIGURATION-SERVERBOUND-OPTIONAL-001"
level = "C4"
state = "configuration"
direction = "serverbound"
patterns = ["minecraft:cookie_response", "minecraft:resource_pack", "minecraft:custom_click_action", "minecraft:accept_code_of_conduct"]
status = "GatedOptional"
responsibility = "Optional"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 1 is identifier plus nullable <=5,120-byte value; ID 6 is UUID plus action 0..7; ID 8 is identifier plus <=65,536-byte optional NBT; ID 9 is unit."]
mappings = ["Cookie/resource-pack/code-of-conduct values are gated connection tasks; custom click maps only to its explicitly server-owned handler."]
transitions = ["Base server rejects cookie response; resource-pack terminal action and conduct accept advance only their matching current task; custom click dispatches without advancing C1."]
ordering = ["Every response requires its owned request/task; unsolicited or wrong-task acknowledgement is a configuration fault, except custom click which is an independently registered action."]
vectors = ["C1-CONFIG-COOKIE-GATE", "C1-CONFIG-RESOURCE-PACK-GATE", "C1-CONFIG-CODE-OF-CONDUCT-GATE", "C1-CONFIG-CUSTOM-CLICK-GATE"]
unknowns = []
reproduction = ["Inject bounded optional responses before, during, and after their locked tasks and verify refusal, UUID/action, NBT, acceptance, and advancement behavior."]
last_commit = "docs(mc-reference): specify C1 configuration protocol"

[[family]]
id = "PROTO-CONFIGURATION-CLIENTBOUND-REQUIRED-001"
level = "C1"
state = "configuration"
direction = "clientbound"
patterns = ["minecraft:custom_payload", "minecraft:disconnect", "minecraft:finish_configuration", "minecraft:keep_alive", "minecraft:ping", "minecraft:registry_data", "minecraft:update_enabled_features", "minecraft:update_tags", "minecraft:select_known_packs"]
status = "Specified"
responsibility = "Required"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 1/2/3/4/5/7/12/13/14 carry bounded payload, component NBT, terminal unit, signed long/int, ordered registry entries, feature identifiers, tag ID maps, and known-pack descriptors."]
mappings = ["Registry entry order defines dynamic wire IDs; tag member VarInts index matching registries; enabled features and known-pack data elision remain connection projection state.", "Brand becomes client telemetry/presentation; disconnect, liveness, and finish do not mutate simulation or persistence."]
transitions = ["Offer starts synchronize_registries; registry/tag packets build client registry access; finish finalizes it and installs clientbound play before acknowledgement; keepalive/ping require exact echoes."]
ordering = ["Brand/features precede offer; response triggers registries then tags; spawn readiness precedes terminal finish; finish precedes all clientbound play packets."]
vectors = ["C1-GOLD-CONFIG-BRAND-CB", "C1-GOLD-CONFIG-FEATURES", "C1-GOLD-CONFIG-KNOWN-OFFER", "C1-GOLD-CONFIG-REGISTRY", "C1-GOLD-CONFIG-TAGS", "C1-GOLD-CONFIG-FINISH", "C1-GOLD-CONFIG-KEEPALIVE", "C1-GOLD-CONFIG-PING", "C1-CONFIG-HAPPY-TRACE", "C1-CONFIG-KNOWN-ORDER", "C1-CONFIG-REGISTRY-BOUNDARIES", "C1-CONFIG-TAG-MAPPINGS", "C1-CONFIG-FEATURE-NAMES", "C1-CONFIG-FINISH-ORDER", "C1-CONFIG-KEEPALIVE-STATE", "C1-CONFIG-CUSTOM-PAYLOAD-BOUNDS"]
unknowns = []
reproduction = ["Generate official-codec goldens, reconstruct all 29 synchronized registries and tag ID maps, then execute happy, malformed, duplicate, timeout, and terminal-order vectors."]
last_commit = "docs(mc-reference): specify C1 configuration protocol"

[[family]]
id = "PROTO-CONFIGURATION-CLIENTBOUND-OPTIONAL-001"
level = "C4"
state = "configuration"
direction = "clientbound"
patterns = ["minecraft:cookie_request", "minecraft:reset_chat", "minecraft:resource_pack_pop", "minecraft:resource_pack_push", "minecraft:store_cookie", "minecraft:transfer", "minecraft:custom_report_details", "minecraft:server_links", "minecraft:clear_dialog", "minecraft:show_dialog", "minecraft:code_of_conduct"]
status = "GatedOptional"
responsibility = "Optional"
owner = "protocol/login-and-configuration"
specification = "login-and-configuration.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 0/6/8..11/15..19 use the exact cookie, unit, resource-pack, transfer, bounded report/link, context-free dialog, and UTF code-of-conduct schemas in the specification."]
mappings = ["Every optional packet affects connection transfer, client resources, reporting metadata, or presentation only; none enters authoritative world state."]
transitions = ["Base fresh configuration omits every gate except configured server links/tasks; transfer closes and reconnects, conduct/resource pack can block, and reset/dialog packets alter client presentation."]
ordering = ["Cookie/resource-pack/conduct replies correlate their request/task; transfer may preserve stored cookies; reset-chat is for play-to-configuration re-entry, not the fresh minimum path."]
vectors = ["C1-CONFIG-COOKIE-GATE", "C1-CONFIG-RESOURCE-PACK-GATE", "C1-CONFIG-CODE-OF-CONDUCT-GATE", "C1-CONFIG-TRANSFER-GATE", "C1-CONFIG-PRESENTATION-GATES"]
unknowns = []
reproduction = ["Drive the locked vanilla client through each optional packet at codec boundaries and verify request correlation, task blocking, transfer, URI validation, and presentation-only effects."]
last_commit = "docs(mc-reference): specify C1 configuration protocol"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-ENTRY-001"
level = "C1"
state = "play"
direction = "serverbound"
patterns = ["minecraft:accept_teleportation"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["ID 0 is one teleport challenge VarInt with no additional fields."]
mappings = ["A matching challenge acknowledges only connection-local position synchronization; the numeric challenge never enters authoritative movement, ECS state or persistence."]
transitions = ["A matching ID clears the one pending correction after snapping authoritative position; a stale nonmatching ID is ignored; a duplicate matching ID after clearance disconnects as invalid movement."]
ordering = ["The vanilla client applies ID-72 player_position, sends ID-0 accept_teleportation, then sends ID-31 move_player_pos_rot; the server resends a pending correction after more than 20 listener ticks."]
vectors = ["C1-GOLD-PLAY-ACCEPT", "C1-PLAY-ENTRY-TRACE", "C1-PLAY-TELEPORT-ORDER", "C1-PLAY-TELEPORT-STALE-DUPLICATE"]
unknowns = []
reproduction = ["Generate the official-codec acknowledgement frame and replay matching, reordered, stale, duplicate and resend cases from the first play position."]
last_commit = "docs(mc-reference): specify C1 play entry"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-MOVEMENT-001"
level = "C2"
state = "play"
direction = "serverbound"
patterns = ["minecraft:chunk_batch_received", "minecraft:client_tick_end", "minecraft:client_information", "minecraft:keep_alive", "minecraft:move_player_pos", "minecraft:move_player_pos_rot", "minecraft:move_player_rot", "minecraft:move_player_status_only", "minecraft:move_vehicle", "minecraft:paddle_boat", "minecraft:player_abilities", "minecraft:player_command", "minecraft:player_input", "minecraft:player_loaded", "minecraft:pong"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 11/13/14/28/30..35/40/42..45 use the exact float, unit, client-information, signed echo, four-form player movement, vehicle, paddle, ability, command, input and terrain-ready grammars in the specification."]
mappings = ["Accepted movement becomes a normalized authoritative collision move; client options and input become bounded session state; chunk advice, liveness, tick boundaries, raw flags, packet/entity IDs and echo payloads remain adapter-local.", "Player/vehicle correction thresholds, exceptional-float handling, collision recovery, floating timeout and idle/known-movement updates reproduce the locked handlers without importing wire structs into ECS persistence."]
transitions = ["ID 44 or expiry opens the 60-tick client-load gate; matching keepalive clears its pending challenge while invalid remote echoes disconnect; chunk feedback acknowledges/floors batch count and expands the scheduling window; client_tick_end closes one known-movement interval."]
ordering = ["The vanilla client sends changed input before movement, one selected player-movement form per active tick (or riding rotation/vehicle), then client_tick_end; player_loaded follows terrain readiness; chunk feedback follows batch-finished; keepalive and ping use disjoint exact echoes."]
vectors = ["C2-GOLD-SERVERBOUND-MOVEMENT", "C2-MOVEMENT-FORMS-CADENCE", "C2-MOVEMENT-VALIDATION", "C2-MOVEMENT-TELEPORT-LOAD-GATES", "C2-MOVEMENT-FLOATING", "C2-VEHICLE-CORRECTION", "C2-INPUT-COMMAND-ABILITIES", "C2-CLIENT-TICK-END", "C2-TERRAIN-READY", "C2-CHUNK-BATCH-FEEDBACK", "C2-PLAY-LIVENESS"]
unknowns = []
reproduction = ["Generate every fixture with the locked Java 25 official codec, trace client movement selection and server validation across load/teleport/collision/speed/non-finite/floating cases, and drive chunk/liveness feedback at every boundary."]
last_commit = "docs(mc-reference): specify C2 movement session"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-BLOCK-001"
level = "C2"
state = "play"
direction = "serverbound"
patterns = ["minecraft:pick_item_from_block", "minecraft:player_action", "minecraft:swing", "minecraft:use_item_on", "minecraft:use_item"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 36/41/63/66/67 use packed block position plus boolean, action/position/modulo-direction/sequence, strict hand, hand/block-hit/sequence, and hand/sequence/yaw/pitch grammars exactly as specified."]
mappings = ["Destroy/use requests map to normalized authoritative interaction commands paired with the source-specified block/item mechanics; pick-block maps a namespaced clone stack into inventory selection; swing and auxiliary actions map only their documented hand/player intent.", "Packed coordinates, enum ordinals, hit offsets, sequence counters, raw item/component identities, correction packet forms and client prediction records remain inside the 26.2 adapter and do not enter ECS persistence."]
transitions = ["Destroy/use predictive requests act only after the client-load gate and register a nonnegative sequence at their path-specific point; auxiliary actions use the loaded gate without ACK; pick and swing have their explicit no-load-gate behavior; every packet is legal only in play."]
ordering = ["Prediction runs before request send; use-on/use register before validation while destroy registers after its handler; the server coalesces the maximum only until next listener tick; immediate use-on/break corrections precede ACK while ordinary successful block deltas may follow it."]
vectors = ["C2-GOLD-SERVERBOUND-BLOCK", "C2-BLOCK-CODEC-BOUNDARIES", "C2-BLOCK-SEQUENCE-LIFECYCLE", "C2-BLOCK-PREDICTION-ORDER", "C2-BLOCK-USE-ON-ADMISSION", "C2-BLOCK-SERVER-SWING-QUIRK", "C2-BLOCK-BREAK-ACTIONS", "C2-BLOCK-AUXILIARY-ACTIONS", "C2-BLOCK-PICK", "C2-BLOCK-USE-IN-AIR", "C2-BLOCK-END-TO-END"]
unknowns = []
reproduction = ["Generate all five goldens with the locked Java 25 codecs, trace client prediction and server ACK registration at every load/validation boundary, then replay pick, auxiliary, break, use-on, use-in-air, correction and exceptional-value vectors against the locked endpoints."]
last_commit = "docs(mc-reference): specify C2 block convergence"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-CHAT-001"
level = "C3"
state = "play"
direction = "serverbound"
patterns = ["minecraft:chat_ack", "minecraft:chat_command", "minecraft:chat_command_signed", "minecraft:chat", "minecraft:chat_session_update", "minecraft:command_suggestion"]
status = "Todo"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify unsigned/signed chat, command and suggestion grammars, last-seen acknowledgements, validation, filtering, spam control, and secure-profile gates."]
reproduction = ["Trace locked chat codecs, signature/session validation and command dispatch with offline and secure-profile branches."]
last_commit = ""

[[family]]
id = "PROTO-PLAY-SERVERBOUND-ENTITY-SESSION-001"
level = "C3"
state = "play"
direction = "serverbound"
patterns = ["minecraft:attack", "minecraft:client_command", "minecraft:interact", "minecraft:pick_item_from_entity", "minecraft:spectator_action", "minecraft:teleport_to_entity"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = "play-serverbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 1/12/26/37/62/64 are entity VarInt; strict action VarInt; entity/zero-fallback hand/LP-vector/boolean; entity/boolean; optional entity VarInt; and UUID respectively, exactly as specified."]
mappings = ["Attack/interact/pick map level-local target numbers to current entities before normalized combat, interaction or inventory commands; client commands map to respawn/stat/gamerule session requests; spectator actions map to camera selection or UUID-targeted authoritative teleport.", "Entity IDs, compact-vector quantization, action/hand ordinals, optional-ID bias, UUID lookup form, packet IDs and response packet structs remain in the 26.2 adapter; authoritative entities, items, damage and respawn state use namespaced/domain identities."]
transitions = ["Attack/interact and near-camera selection require the client-loaded and documented mode/range gates; pick has no client-loaded gate; UUID teleport requires spectator only; client command always resets idle and branches to dead/win respawn, dirty statistics or permission-gated gamerules."]
ordering = ["The vanilla client sends carried-slot convergence before attack/interact, predicts those actions locally after send, and uses no acknowledgement counter; camera selection relocates before set-camera publication, while respawn command leads to respawn then position and state reprojection."]
vectors = ["C3-GOLD-SERVERBOUND-ENTITY-SESSION", "C3-ENTITY-INGRESS-CODECS", "C3-ATTACK-ADMISSION", "C3-INTERACT-ADMISSION", "C3-PICK-ENTITY", "C3-SPECTATOR-CAMERA", "C3-CLIENT-COMMAND", "C3-ENTITY-SESSION-END-TO-END"]
unknowns = []
reproduction = ["Generate all six goldens through the locked Java 25 codecs, then replay compact-vector, enum, reach, mode, permission, invalid-target, respawn and response-order boundaries against the locked client/server handlers."]
last_commit = "docs(mc-reference): specify C3 entity session"

[[family]]
id = "PROTO-PLAY-SERVERBOUND-INVENTORY-001"
level = "C3"
state = "play"
direction = "serverbound"
patterns = ["minecraft:bundle_item_selected", "minecraft:container_button_click", "minecraft:container_click", "minecraft:container_close", "minecraft:container_slot_state_changed", "minecraft:edit_book", "minecraft:place_recipe", "minecraft:recipe_book_change_settings", "minecraft:recipe_book_seen_recipe", "minecraft:rename_item", "minecraft:seen_advancements", "minecraft:select_trade", "minecraft:set_beacon", "minecraft:set_carried_item", "minecraft:sign_update"]
status = "Todo"
responsibility = "Required"
owner = "protocol/play-serverbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify container/state IDs, slot deltas, recipes, books, signs, trading, beacon, hotbar, advancement and bundle operations with resynchronization behavior."]
reproduction = ["Trace locked inventory and UI codecs and listener validation against source-specified item/container rules."]
last_commit = ""

[[family]]
id = "PROTO-PLAY-SERVERBOUND-ADMIN-001"
level = "C4"
state = "play"
direction = "serverbound"
patterns = ["minecraft:block_entity_tag_query", "minecraft:change_difficulty", "minecraft:change_game_mode", "minecraft:debug_subscription_request", "minecraft:entity_tag_query", "minecraft:jigsaw_generate", "minecraft:lock_difficulty", "minecraft:set_command_block", "minecraft:set_command_minecart", "minecraft:set_creative_mode_slot", "minecraft:set_game_rule", "minecraft:set_jigsaw_block", "minecraft:set_structure_block", "minecraft:set_test_block", "minecraft:test_instance_block_action"]
status = "Todo"
responsibility = "Optional"
owner = "protocol/play-serverbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify permission-gated debug, operator, creative, command-block, structure, jigsaw, test and gamerule requests and their refusal behavior."]
reproduction = ["Trace each locked administrative codec and permission/feature branch with unprivileged and privileged players."]
last_commit = ""

[[family]]
id = "PROTO-PLAY-SERVERBOUND-OPTIONAL-001"
level = "C4"
state = "play"
direction = "serverbound"
patterns = ["minecraft:configuration_acknowledged", "minecraft:cookie_response", "minecraft:custom_payload", "minecraft:ping_request", "minecraft:resource_pack", "minecraft:custom_click_action"]
status = "Todo"
responsibility = "Optional"
owner = "protocol/play-serverbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify play-to-configuration acknowledgement, cookies, payloads, diagnostics, resource packs and custom-click gates with common-listener reuse."]
reproduction = ["Trace locked common/play listeners and state switches for every optional request, response and unsolicited input."]
last_commit = ""

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-ENTRY-001"
level = "C1"
state = "play"
direction = "clientbound"
patterns = ["minecraft:change_difficulty", "minecraft:commands", "minecraft:entity_event", "minecraft:game_event", "minecraft:initialize_border", "minecraft:login", "minecraft:player_abilities", "minecraft:player_info_update", "minecraft:player_position", "minecraft:recipe_book_add", "minecraft:recipe_book_settings", "minecraft:server_data", "minecraft:set_default_spawn_position", "minecraft:set_held_slot", "minecraft:set_time", "minecraft:ticking_state", "minecraft:ticking_step", "minecraft:update_recipes"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 10/16/34/38/43/49/64/70/72/74/76/86/97/105/113/127/128/133 use the exact join-state, command-tree, event, border, registry-aware login, ability, player-list, position, recipe, server-data, spawn, clock and tick grammars in the specification."]
mappings = ["Dynamic dimension-type/world-clock IDs and static item, recipe-display, slot-display, recipe-category and command-argument IDs resolve through the locked registries; player/entity IDs and teleport IDs remain session-local.", "Join projection initializes client-only level, permissions, recipes, player list, border, clocks, spawn and presentation without importing packet structs or numeric IDs into simulation or persistence."]
transitions = ["Login creates the client level; player_position creates one teleport challenge; level-chunks-load-start opens the terrain readiness gate; ticking state/step synchronize the client clock controller."]
ordering = ["The locked server emits login first, core join projections in PlayerList order, position before its acknowledgement, then border/time/spawn/load-start/ticking state and step before asynchronous chunk batches."]
vectors = ["C1-GOLD-PLAY-LOGIN-MINIMAL", "C1-GOLD-PLAY-DIFFICULTY", "C1-GOLD-PLAY-ABILITIES", "C1-GOLD-PLAY-HELD", "C1-GOLD-PLAY-COMMANDS-EMPTY", "C1-GOLD-PLAY-RECIPE-SETTINGS", "C1-GOLD-PLAY-RECIPE-ADD-EMPTY", "C1-GOLD-PLAY-RECIPES-EMPTY", "C1-GOLD-PLAY-PLAYER-INFO-EMPTY", "C1-GOLD-PLAY-POSITION-ZERO", "C1-GOLD-PLAY-GAME-LOAD-START", "C1-GOLD-PLAY-BORDER-DEFAULT", "C1-GOLD-PLAY-SPAWN", "C1-GOLD-PLAY-TIME-EMPTY", "C1-GOLD-PLAY-TICKING", "C1-GOLD-PLAY-TICK-STEP", "C1-PLAY-ENTRY-TRACE", "C1-PLAY-LOGIN-BOUNDARIES", "C1-PLAY-COMMAND-TREE", "C1-PLAY-PLAYER-INFO", "C1-PLAY-RECIPE-STATE", "C1-PLAY-TELEPORT-ORDER"]
unknowns = []
reproduction = ["Generate official-codec default/empty frames, trace a new offline player through PlayerList placement, and replay malformed registry IDs, trees, lists, events and teleport order against the locked client."]
last_commit = "docs(mc-reference): specify C1 play entry"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-TERRAIN-001"
level = "C2"
state = "play"
direction = "clientbound"
patterns = ["minecraft:bundle_delimiter", "minecraft:chunk_batch_finished", "minecraft:chunk_batch_start", "minecraft:chunks_biomes", "minecraft:forget_level_chunk", "minecraft:level_chunk_with_light", "minecraft:light_update", "minecraft:set_chunk_cache_center", "minecraft:set_chunk_cache_radius", "minecraft:set_simulation_distance"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 0/11..13/37/45/48/94/95/111 use the exact unit delimiter, batch, bounded biome-list, packed unload, dimension-sized chunk/palette/block-entity/light, incremental light and signed cache-control grammars in the specification."]
mappings = ["Block-state raw IDs resolve through the locked 32,366-state table, biome IDs through the frozen configuration registry, block-entity IDs through the 49-entry static registry, and heightmap IDs through the locked enum/fallback rules in registry-and-metadata-mappings.md.", "Chunk snapshots, palette data, heightmaps, block-entity tags and lighting project authoritative terrain into client cache/render state; raw IDs, masks, indices, batch counters and cache coordinates remain version-adapter state."]
transitions = ["Bundle delimiters create one bounded synthetic bundle; cache controls change view projection; start/full-chunk/finish plus serverbound feedback advances batch flow; full chunk installs cache and queues light; biome refresh replaces present chunk biomes; forget drops chunk/debug/light state; compiled terrain or readiness exemptions trigger player_loaded."]
ordering = ["A changed cache center precedes tracking-set difference; each nonempty terrain batch is start, nearest pending full chunks, finish, then feedback; full chunk data installs before its queued light application; unload removes sent chunks and all light; batch finish alone does not mark terrain ready."]
vectors = ["C2-GOLD-CLIENTBOUND-TERRAIN", "C2-BUNDLE-BOUNDARIES", "C2-CACHE-INTEREST", "C2-CHUNK-BATCH-TRACE", "C2-FULL-CHUNK-PALETTES", "C2-CHUNK-HEIGHTMAP-BLOCK-ENTITIES", "C2-LIGHT-MASKS", "C2-BIOME-REFRESH", "C2-CHUNK-UNLOAD", "C2-TERRAIN-READY-TRACE"]
unknowns = []
reproduction = ["Generate simple bodies with the locked Java 25 official codecs; capture a locked-server initial terrain trace; replay every palette, registry, section-count, NBT, light-mask, cache, batch, biome, unload and readiness boundary against the locked client."]
last_commit = "docs(mc-reference): specify C2 terrain stream"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-BLOCK-001"
level = "C2"
state = "play"
direction = "clientbound"
patterns = ["minecraft:block_changed_ack", "minecraft:block_destruction", "minecraft:block_entity_data", "minecraft:block_event", "minecraft:block_update", "minecraft:section_blocks_update"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 4..8/84 use signed ACK, breaker/packed-position/unsigned progress, packed-position/static type/trusted compound, packed-position/two unsigned bytes/static block, packed-position/global state, and packed section/count/VarLong change grammars exactly as specified."]
mappings = ["IDs 8/84 resolve only the locked 32,366-state table, ID 7 the distinct 1,196-block registry, and ID 6 the 49-type block-entity registry; block entities apply only to a matching current typed entity.", "Authoritative states and tags project normalized world snapshots; ACKs release client-retained states; cracks/events remain presentation; all raw IDs, packed coordinates, stages and sequence records stay adapter-local."]
transitions = ["ACK releases every retained latest sequence at or below its value without range/monotonic validation; state deltas stage behind retained predictions or write immediately; block-entity mismatch/cache misses follow explicit ignore paths; malformed mappings fault."]
ordering = ["Use-on and denial corrections can precede next-tick ACK; successful writes can be ACKed before later chunk deltas; changed light precedes block deltas; one/multi-position state packets precede matching block-entity packets; crack/event streams do not acknowledge prediction."]
vectors = ["C2-GOLD-CLIENTBOUND-BLOCK", "C2-BLOCK-CODEC-BOUNDARIES", "C2-BLOCK-SEQUENCE-LIFECYCLE", "C2-BLOCK-PREDICTION-ORDER", "C2-BLOCK-DELTA-MAPPINGS", "C2-BLOCK-ENTITY-DATA", "C2-BLOCK-EVENTS", "C2-BLOCK-DESTRUCTION-PROGRESS", "C2-BLOCK-END-TO-END"]
unknowns = []
reproduction = ["Generate all six goldens with the locked Java 25 codecs and registry bootstrap, then replay ACK/update order, state/type registry boundaries, trusted NBT, cache misses, section duplicates, block events and crack progress against the locked client and server broadcaster."]
last_commit = "docs(mc-reference): specify C2 block convergence"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-SESSION-001"
level = "C2"
state = "play"
direction = "clientbound"
patterns = ["minecraft:disconnect", "minecraft:keep_alive", "minecraft:move_vehicle", "minecraft:ping", "minecraft:player_rotation"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 32/44/57/61/73 use trusted context-free reason NBT, signed-long keepalive, absolute vehicle pose, signed-int ping, and two-boolean relative rotation grammars in the specification."]
mappings = ["Disconnect/liveness are connection-local projections; player rotation and vehicle correction derive from authoritative state but raw IDs, payloads, local vehicle identity and packet structs remain version-adapter state."]
transitions = ["Disconnect closes with the supplied reason; keepalive schedules an exact serverbound ID-28 echo; ping schedules ID-45; player rotation applies immediately and returns ID-32; qualifying vehicle correction snaps only on positional difference and always returns ID-34."]
ordering = ["Keepalive and ping have disjoint echo domains; rotation response follows application without a teleport challenge; vehicle correction applies only to the current locally authoritative root vehicle and its echo enters normal server validation."]
vectors = ["C2-GOLD-CLIENTBOUND-SESSION", "C2-PLAY-DISCONNECT", "C2-PLAY-LIVENESS", "C2-PLAYER-ROTATION", "C2-VEHICLE-CORRECTION-CLIENT", "C2-SESSION-EXCEPTIONAL-FLOATS"]
unknowns = []
reproduction = ["Generate every non-NBT fixture with the locked Java 25 official codec and replay disconnect, frozen-render liveness, relative rotation, irrelevant/current vehicle and exceptional-float branches against the locked client."]
last_commit = "docs(mc-reference): specify C2 movement session"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-ENTITY-SESSION-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:animate", "minecraft:damage_event", "minecraft:hurt_animation", "minecraft:respawn", "minecraft:set_camera", "minecraft:take_item_entity"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 2/25/42/82/93/124 carry entity/action, entity/dynamic damage type/biased causes/optional position, entity/yaw, common spawn/keep mask, camera entity, and source/collector/amount grammars exactly as specified."]
mappings = ["Damage type and respawn dimension type resolve through the frozen configuration registries; all entity numbers are level-local projection keys and damage source position takes precedence over cause/direct lookups.", "Damage, animation, camera, pickup and respawn packets project authoritative outcomes into client presentation/session state; entity IDs, holder raw IDs, masks and packet structs remain adapter-local rather than ECS or persistence identities."]
transitions = ["Unknown entity numbers follow each handler's ignore/fallback rule; valid damage and animation update presentation, camera changes only for a present entity, pickup mutates/removes only a present source, and respawn replaces local player/possibly level while reopening the player-loaded gate."]
ordering = ["Damage event is emitted only on the full unblocked damage-event branch and hurt yaw separately targets the damaged player; set-camera follows server relocation; respawn precedes position correction and the remaining spawn, difficulty, experience, effect, level and permission projections."]
vectors = ["C3-GOLD-CLIENTBOUND-ENTITY-SESSION", "C3-ENTITY-FEEDBACK", "C3-DAMAGE-PROJECTION", "C3-RESPAWN-SESSION", "C3-SPECTATOR-CAMERA", "C3-TAKE-ITEM", "C3-ENTITY-SESSION-END-TO-END"]
unknowns = []
reproduction = ["Generate all six goldens through locked Java 25 codecs with explicit damage/dimension registry snapshots, then replay entity absence/type, action byte, damage-source, keep-mask, camera, pickup amount and respawn-order boundaries."]
last_commit = "docs(mc-reference): specify C3 entity session"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-ENTITY-SPAWN-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:add_entity", "minecraft:remove_entities"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 1/77 carry entity ID, UUID, defaulted static entity-type raw ID, absolute pose, compact motion, three byte rotations and type-specific data; and a signed VarInt count followed by signed entity VarInts exactly as specified."]
mappings = ["The 158-entry static entity-type registry is locked by reports/registries.json; numeric entity and projectile-owner IDs are current-level projection keys, UUID projects normalized authoritative entity/profile identity, and falling-block data resolves only through the 32,366-entry state table.", "Spawn data, packed rotations, raw registry IDs and removal lists remain adapter/session state; Ferrite emits them from namespaced entity types and normalized authoritative relationships without persisting wire numbers."]
transitions = ["Add clears a matching former-vehicle marker before construction, enforces prior player info and client spawn checks, recreates type-specific state before same-ID replacement, then inserts/sounds/marks seen players; removal processes wire order, retains a qualifying player vehicle, discards present targets and drops their debug projections."]
ordering = ["A tracker pairing bundle orders add first, then nondefault metadata, syncable attributes, nonempty equipment, own/vehicle passenger lists and leash; player info must already exist, while unpairing sends canonical one-ID removal after stopSeenByPlayer."]
vectors = ["C3-GOLD-CLIENTBOUND-ENTITY-SPAWN", "C3-ENTITY-SPAWN-CODECS", "C3-ENTITY-TYPE-MAPPING", "C3-ENTITY-CREATION", "C3-ENTITY-SPAWN-DATA", "C3-ENTITY-REMOVAL", "C3-ENTITY-PAIRING-ORDER", "C3-ENTITY-SPAWN-END-TO-END"]
unknowns = []
reproduction = ["Generate both goldens through locked Java 25 codecs with built-in entity type zero, enumerate all recreate/add-packet overrides, then replay registry, player-info, spawn-check, data, replacement/duplicate, vehicle and removal-order boundaries."]
last_commit = "docs(mc-reference): specify C3 entity spawn"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-ENTITY-MOTION-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:entity_position_sync", "minecraft:move_entity_pos", "minecraft:move_entity_pos_rot", "minecraft:move_minecart_along_track", "minecraft:move_entity_rot", "minecraft:rotate_head", "minecraft:set_entity_motion", "minecraft:teleport_entity", "minecraft:projectile_power"]
status = "Specified"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = "play-clientbound.md"
evidence = ["OFF-SERVER-001", "OFF-CLIENT-001", "OFF-REPORT-001"]
fields = ["IDs 35/53/54/55/56/83/101/125/135 carry absolute position-motion-rotation, signed-short relative deltas, byte rotations, minecart step lists, compact motion, relative-mask teleport and projectile power exactly as specified."]
mappings = ["Entity IDs and position-codec bases are current-level projection state; relative masks and packed rotations are adapter values, while authoritative position, velocity, rotation, ground state and projectile acceleration remain normalized simulation values.", "Minecart steps are enabled-feature presentation queues and neither their packet records nor interpolation weights enter ECS or persistence state."]
transitions = ["Missing targets are ignored except the retained removed-player-vehicle teleport fallback; local-authoritative entities consume relative base updates without pose/ground application, remote entities snap or interpolate by the documented packet path, and matching projectile/minecart runtime types receive their specialized projections."]
ordering = ["Tracker motion precedes its position/rotation packet, dirty data follows it, head rotation follows dirty data, and hurt-marked self-inclusive motion is last; riding-player teleport applies then returns the documented player/vehicle movement echo only on its fallback/authoritative branches."]
vectors = ["C3-GOLD-CLIENTBOUND-ENTITY-MOTION", "C3-ENTITY-MOTION-CODECS", "C3-RELATIVE-ENTITY-MOVEMENT", "C3-ABSOLUTE-ENTITY-SYNC", "C3-ENTITY-TELEPORT", "C3-MINECART-STEP-QUEUE", "C3-ENTITY-MOTION-PUBLICATION", "C3-ENTITY-MOTION-END-TO-END"]
unknowns = []
reproduction = ["Generate all nine goldens with the locked Java 25 official codecs, then replay missing/local-authoritative/interpolating entities, short-delta endpoints, relative masks, former vehicles, minecart weights and projectile runtime-type boundaries."]
last_commit = "docs(mc-reference): specify C3 entity motion"

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-ENTITY-STATE-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:set_entity_data", "minecraft:set_entity_link", "minecraft:set_equipment", "minecraft:set_passengers", "minecraft:update_attributes"]
status = "Todo"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify metadata serializer/slot tables, leash and passenger relations, equipment and dynamic attribute projections."]
reproduction = ["Trace locked serializer registration, every entity metadata accessor, attribute/equipment codecs, client relation handlers and tracker pairing/update order."]
last_commit = ""

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-ENTITY-EFFECTS-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:explode", "minecraft:remove_mob_effect", "minecraft:update_mob_effect"]
status = "Todo"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify explosion result and mob-effect add/update/remove projections with dynamic effect and particle mappings."]
reproduction = ["Trace locked explosion/effect broadcasters and client handlers across registry, duration, blend, particle and entity-absence boundaries."]
last_commit = ""

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-INVENTORY-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:award_stats", "minecraft:container_close", "minecraft:container_set_content", "minecraft:container_set_data", "minecraft:container_set_slot", "minecraft:cooldown", "minecraft:map_item_data", "minecraft:merchant_offers", "minecraft:mount_screen_open", "minecraft:open_book", "minecraft:open_screen", "minecraft:open_sign_editor", "minecraft:place_ghost_recipe", "minecraft:recipe_book_remove", "minecraft:set_cursor_item", "minecraft:set_experience", "minecraft:set_health", "minecraft:set_player_inventory", "minecraft:tag_query", "minecraft:update_advancements"]
status = "Todo"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify inventory/menu state, item stacks/components, recipes, stats, health/experience, maps, trades, tag queries and progression projections with state-ID convergence."]
reproduction = ["Trace locked server menu synchronizers and client handlers against source-specified inventory and progression rules."]
last_commit = ""

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-CHAT-SCORE-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:boss_event", "minecraft:clear_titles", "minecraft:command_suggestions", "minecraft:custom_chat_completions", "minecraft:delete_chat", "minecraft:disguised_chat", "minecraft:player_chat", "minecraft:player_combat_end", "minecraft:player_combat_enter", "minecraft:player_combat_kill", "minecraft:player_info_remove", "minecraft:player_look_at", "minecraft:reset_score", "minecraft:select_advancements_tab", "minecraft:set_action_bar_text", "minecraft:set_display_objective", "minecraft:set_objective", "minecraft:set_player_team", "minecraft:set_score", "minecraft:set_subtitle_text", "minecraft:set_title_text", "minecraft:set_titles_animation", "minecraft:system_chat", "minecraft:tab_list", "minecraft:waypoint"]
status = "Todo"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify chat/signature presentation, commands, player list, combat notices, scoreboards, bosses, titles, tab content, advancement selection and waypoint projections."]
reproduction = ["Trace locked server broadcasters and client presentation handlers, including ordering and malformed component/signature behavior."]
last_commit = ""

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-WORLD-EFFECT-001"
level = "C3"
state = "play"
direction = "clientbound"
patterns = ["minecraft:level_event", "minecraft:level_particles", "minecraft:set_border_center", "minecraft:set_border_lerp_size", "minecraft:set_border_size", "minecraft:set_border_warning_delay", "minecraft:set_border_warning_distance", "minecraft:sound_entity", "minecraft:sound", "minecraft:stop_sound"]
status = "Todo"
responsibility = "Required"
owner = "protocol/play-clientbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify world-border deltas and registry-backed level event, particle and sound projections with positional encodings and presentation-only behavior."]
reproduction = ["Trace locked world effect broadcasters and client handlers across registry, distance, seed and stop-category cases."]
last_commit = ""

[[family]]
id = "PROTO-PLAY-CLIENTBOUND-OPTIONAL-001"
level = "C4"
state = "play"
direction = "clientbound"
patterns = ["minecraft:cookie_request", "minecraft:custom_payload", "minecraft:debug/block_value", "minecraft:debug/chunk_value", "minecraft:debug/entity_value", "minecraft:debug/event", "minecraft:debug_sample", "minecraft:game_rule_values", "minecraft:game_test_highlight_pos", "minecraft:low_disk_space_warning", "minecraft:pong_response", "minecraft:resource_pack_pop", "minecraft:resource_pack_push", "minecraft:start_configuration", "minecraft:store_cookie", "minecraft:test_instance_block_status", "minecraft:transfer", "minecraft:update_tags", "minecraft:custom_report_details", "minecraft:server_links", "minecraft:clear_dialog", "minecraft:show_dialog"]
status = "Todo"
responsibility = "Optional"
owner = "protocol/play-clientbound"
specification = ""
evidence = ["OFF-REPORT-001"]
fields = []
mappings = []
transitions = []
ordering = []
vectors = []
unknowns = ["Specify reconfiguration, cookies, payload/resource/transfer/dialog/report gates, diagnostics, debug/test projections, gamerule inspection, ticking step and low-disk presentation."]
reproduction = ["Trace locked common/game sends and client handlers for every optional packet, request correlation and feature/permission gate."]
last_commit = ""
