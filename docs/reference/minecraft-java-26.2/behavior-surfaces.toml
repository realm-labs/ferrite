version = "26.2"

# This ledger inventories the root ways observable behavior can enter or leave the game model.
# Mapped means that the root boundary has an explicit inventory and owners; it does not override
# Todo, InProgress, Unreviewed, or SourceInconclusive work in the referenced ledgers.

[[surface]]
id = "SURFACE-TICK-SCHEDULER-001"
kind = "TickScheduler"
boundary = "Server ticks, per-level phases, scheduled work, random activity and pause controls."
triggers = [
  "fixed server tick",
  "scheduled block or fluid due time",
  "random tick sample",
  "freeze, step, sprint and empty-server controls",
]
inventory_sources = ["OfficialServerSymbols", "RegistryReport"]
selectors = [
  "MinecraftServer and ServerLevel tick roots",
  "block and fluid scheduled-tick queues",
  "chunk activity and ticket boundaries",
  "all random-tick and tick-rate controls",
]
owners = ["SIM-001", "SIM-002", "SIM-003", "SIM-004", "SIM-005", "SIM-006", "BLK-TEST-INSTANCE-001", "ITM-HOPPER-001", "ITM-DISPENSER-001"]
state_domains = [
  "server clock and tick-rate state",
  "dimension insertion order",
  "chunk activity and scheduled queues",
  "hopper per-tick cooldown, push-before-pull transfer and collision-triggered reuse of that transaction",
  "dispenser retained four-tick scheduled dispatch and captured-state behavior transaction",
  "test-instance RUN reset, in-place GameTest runner replacement, setup delay and result callbacks",
]
persistence = [
  "scheduled work survives save and reload through the SIM-003 boundary",
  "server and world clocks use the SIM-006 continuity rules",
  "dispenser scheduled work and persisted triggered state remain separate continuity domains",
  "test-instance runner replacement swaps the packet-owned entity for a fresh authoritative record",
]
client_projection = [
  "time, weather and committed world changes are projected after authoritative phases",
  "client render and input ticks remain a separate CLI-001 time domain",
  "hopper inventory projects through menu/comparator effects while cooldown and tick time remain server-private",
  "dispenser due work projects through authoritative inventory/block/entity mutation and ordered level events",
  "test-instance status, markers and runner results project after their authoritative callbacks",
]
protocol_families = []
status = "Mapped"
evidence = ["OFF-SERVER-001", "OFF-REPORT-001", "EXP-SIM-001", "EXP-SIM-002"]
unknowns = []
reproduction = [
  "Re-run EXP-SIM-001 through EXP-SIM-005 after any tick-root or activity-boundary change.",
]
last_commit = "docs(mc-reference): specify test instance block runtime"

[[surface]]
id = "SURFACE-NETWORK-INGRESS-001"
kind = "NetworkIngress"
boundary = "Every serverbound packet that can admit, reject or order an observable state change."
triggers = [
  "handshake, login, configuration and play packets",
  "movement, interaction, inventory, chat and common-service acknowledgements",
  "reconfiguration and disconnect transitions",
]
inventory_sources = ["PacketReport", "OfficialServerSymbols"]
selectors = [
  "all 22 serverbound protocol families, partitioned exactly in network-ingress-roots.md",
  "packet listener dispatch and connection-state transitions",
  "sequence, state-ID, teleport and common-service acknowledgement boundaries",
]
owners = [
  "BLK-COMMAND-001",
  "BLK-JIGSAW-001",
  "BLK-STRUCTURE-001",
  "BLK-AIR-001",
  "BLK-TEST-BLOCK-001",
  "BLK-TEST-INSTANCE-001",
  "BLK-BEACON-001",
  "BLK-SIGN-001",
  "ITM-CHEST-001",
  "ITM-HOPPER-001",
  "ITM-DISPENSER-001",
  "PLY-001",
  "PLY-005",
  "PLY-006",
  "ITM-002",
  "ENT-005",
  "WGEN-005",
  "CLI-003",
  "CLI-004",
  "CLI-005",
]
state_domains = [
  "connection state and authenticated player session",
  "authoritative player, world, entity and container state",
  "AIR item sentinel normalization for creative-slot and container-hash ingress",
  "operator jigsaw edit fields and immediate-generation admission",
  "operator structure-block record commit, action validation and template-manager mutation",
  "operator test-block mode/message edit and nested state projection",
  "operator test-instance record/action admission, template mutation and GameTest runner handoff",
  "beacon payment and power selection against handler-time menu, level and tag state",
  "sign-update filtering completion against current chunk, side, wax and editor authorization",
  "chest use admission, single/double menu lifetime and container convergence",
  "hopper menu admission/click/close convergence against concurrent automation ticks",
  "dispenser generic_3x3 menu convergence against redstone-scheduled inventory mutation",
  "acknowledgement and correction sequences",
]
persistence = [
  "session transport state is not persisted",
  "accepted gameplay mutations flow to their owning persistent state domains",
  "decoded AIR item values never create a durable positive stack",
]
client_projection = [
  "rejection and convergence packets expose the admitted authoritative result",
  "AIR creative-slot input converges as an empty slot and supplies no dropped item",
  "test-instance updates and positionless status responses expose accepted edits and action results",
  "disconnect and reconfiguration change the client's active protocol state",
]
protocol_families = [
  "PROTO-HANDSHAKE-SERVERBOUND-001",
  "PROTO-STATUS-SERVERBOUND-001",
  "PROTO-LOGIN-SERVERBOUND-REQUIRED-001",
  "PROTO-LOGIN-SERVERBOUND-OPTIONAL-001",
  "PROTO-CONFIGURATION-SERVERBOUND-REQUIRED-001",
  "PROTO-CONFIGURATION-SERVERBOUND-OPTIONAL-001",
  "PROTO-PLAY-SERVERBOUND-ENTRY-001",
  "PROTO-PLAY-SERVERBOUND-MOVEMENT-001",
  "PROTO-PLAY-SERVERBOUND-BLOCK-001",
  "PROTO-PLAY-SERVERBOUND-CHAT-001",
  "PROTO-PLAY-SERVERBOUND-ENTITY-SESSION-001",
  "PROTO-PLAY-SERVERBOUND-CONTAINER-CONVERGENCE-001",
  "PROTO-PLAY-SERVERBOUND-SIGN-UPDATE-001",
  "PROTO-PLAY-SERVERBOUND-RECIPE-BOOK-001",
  "PROTO-PLAY-SERVERBOUND-MERCHANT-001",
  "PROTO-PLAY-SERVERBOUND-ANVIL-BEACON-001",
  "PROTO-PLAY-SERVERBOUND-INVENTORY-AUXILIARY-001",
  "PROTO-PLAY-SERVERBOUND-ADMIN-STATE-001",
  "PROTO-PLAY-SERVERBOUND-OPERATOR-BLOCKS-001",
  "PROTO-PLAY-SERVERBOUND-DEBUG-SUBSCRIPTION-001",
  "PROTO-PLAY-SERVERBOUND-COMMON-SERVICES-001",
  "PROTO-PLAY-SERVERBOUND-RECONFIGURATION-001",
]
status = "Mapped"
evidence = ["OFF-REPORT-001", "OFF-SERVER-001", "PROTO-PLAY-SERVERBOUND-ENTRY-001"]
unknowns = []
reproduction = [
  """
Regenerate the packet report, verify the exact serverbound-family partition, then run every
family's protocol vectors and one admission, rejection and interleaving vector per semantic join.
""",
]
last_commit = "docs(mc-reference): specify air identity runtime"

[[surface]]
id = "SURFACE-COMMAND-ADMINISTRATION-001"
kind = "CommandAdministration"
boundary = "Commands and operator controls that mutate world, player, rule or server state."
triggers = [
  "command dispatcher execution",
  "operator blocks and administrative packets",
  "game-rule, difficulty, time, weather, border and player-management changes",
]
inventory_sources = ["CommandReport", "OfficialServerSymbols", "PacketReport"]
selectors = [
  "all 92 roots in the locked commands report, partitioned exactly once by command-roots.toml",
  "permission and operator-only branches",
  "command result, feedback and side-effect ordering",
]
owners = [
  "SIM-006",
  "BLK-003",
  "BLK-COMMAND-001",
  "BLK-TEST-BLOCK-001",
  "BLK-TEST-INSTANCE-001",
  "CLI-COMMAND-FEEDBACK-001",
  "SIM-COMMAND-LIMIT-001",
  "BLK-COMMAND-AREA-001",
  "ENV-004",
  "WGEN-006",
  "PLY-001",
  "ENT-008",
]
state_domains = [
  "server configuration and permissions",
  "world, player, entity, time, weather and border state",
  "command result and feedback channels",
  "snapshotted command-action and redirect budgets",
  "bounded area-command block, block-entity, scheduled-tick and biome transactions",
  "operator test-block edit, redstone latch and block-based test outcome state",
  "operator test-instance edit, template-volume mutation and GameTest outcome state",
]
persistence = [
  "persistent command effects must join the same save boundary as direct gameplay mutations",
  "command-block carriers save command, result and trigger state through BLK-COMMAND-001",
  "test blocks save mode, message and powered while their trigger latch is transient",
  "test-instance data and markers persist while a successful RUN replaces the edited entity record",
  "ephemeral feedback and query results are not persisted",
]
client_projection = [
  "command feedback, system chat and admin presentation packets",
  "command-block edits and tracked output use carrier-specific block or entity update hooks",
  "test-block edits use block-state and block-entity convergence before later test outcomes",
  "test-instance edits, action feedback, status packets and runner results have distinct projection points",
  "ordinary world and player convergence for successful mutations",
]
protocol_families = [
  "PROTO-PLAY-SERVERBOUND-ADMIN-STATE-001",
  "PROTO-PLAY-SERVERBOUND-OPERATOR-BLOCKS-001",
  "PROTO-PLAY-CLIENTBOUND-ADMIN-PRESENTATION-001",
]
status = "InProgress"
evidence = ["OFF-REPORT-001", "OFF-SERVER-001"]
unknowns = [
  """
The 92 command roots now have exhaustive broad state-domain and rule ownership, but executable
leaves, aliases, permission predicates, per-target partial results, mutation/persistence boundaries
and feedback/projection order remain explicitly unaudited in each command-roots.toml family.
""",
]
reproduction = [
  """
Regenerate commands.json and require command-roots.toml to partition its root children exactly
once; then expand one recoverable family at a time through dispatcher registration and execution
symbols until every executable leaf is mapped to a behavior rule or a proven read-only path.
""",
]
last_commit = "docs(mc-reference): specify test instance block runtime"

[[surface]]
id = "SURFACE-CONTENT-DISPATCH-001"
kind = "ContentDispatch"
boundary = "Registry-selected block, item, entity, effect, recipe, loot and worldgen control flow."
triggers = [
  "registered content hook or codec dispatch",
  "data-driven selector resolving to executable behavior",
  "generic family behavior parameterized by locked data",
]
inventory_sources = ["RegistryReport", "BundledData", "OfficialServerSymbols"]
selectors = [
  "all nine dispatch and recovery families in content-dispatch-roots.md",
  "all 9,078 IDs and four fallback families in catalog/catalog.toml",
  "all implementation-class, codec, tag, component and bundled-data dispatch boundaries",
]
owners = [
  "BLK-001",
  "BLK-002",
  "BLK-007",
  "BLK-BRUSHABLE-001",
  "BLK-SCULK-SENSOR-001",
  "BLK-JIGSAW-001",
  "BLK-STRUCTURE-001",
  "BLK-STRUCTURE-VOID-001",
  "BLK-AIR-001",
  "BLK-BEDROCK-001",
  "BLK-REINFORCED-DEEPSLATE-001",
  "BLK-TINTED-GLASS-001",
  "BLK-GLASS-001",
  "BLK-SLIME-001",
  "BLK-HONEY-001",
  "BLK-SOUL-SAND-001",
  "BLK-TEST-BLOCK-001",
  "BLK-TEST-INSTANCE-001",
  "BLK-CONDUIT-001",
  "BLK-BEACON-001",
  "BLK-SIGN-001",
  "BLK-SKULL-001",
  "BLK-COMMAND-001",
  "ITM-001",
  "ITM-003",
  "ITM-HONEYCOMB-001",
  "ITM-CHEST-001",
  "ITM-HOPPER-001",
  "ITM-DISPENSER-001",
  "ENT-001",
  "ENT-004",
  "MOB-004",
  "MOB-UNIVERSAL-ANGER-001",
  "MOB-RAID-001",
  "ENT-ENTITY-DROPS-001",
  "WGEN-003",
]
state_domains = [
  "block and block-entity state",
  "brush use, dust progress, archaeology content, cooldown and reset state",
  "vibration candidates, travel, sensor phases, frequency and directional redstone state",
  "jigsaw orientation, connector edit fields and operator generation handoff",
  "structure mode, editable record, corner scan, template-manager and redstone action state",
  "structure-void replacement/shape state plus fluid, capture, jigsaw and debug-render ID dispatch",
  "three air block identities, empty-state consumers and the AIR item empty-stack sentinel",
  "bedrock identity, zero-progress/full-cube properties and direct/tag-selected entity, environment and generation dispatch",
  "reinforced-deepslate identity, positive-hardness/no-loot properties and piston, explosion, boss, feature, creative-tab and model dispatch",
  "tinted-glass identity, glass-derived shapes/strength, light, loot, spawn, beacon, recipe, tag and model dispatch",
  "plain-glass identity, shapes/strength, light, Silk Touch loot, spawn, beacon, smelting, tag and face-culling dispatch",
  "slime-block identity, shapes/properties, collision hooks, piston adhesion/entity motion, loot, recipes, brewing, debug generation and model dispatch",
  "honey-block identity, shapes/properties, fall/slide hooks, piston adhesion/surface carrying, tags, AI, loot, recipes, debug generation and model dispatch",
  "soul-sand identity, split shapes/properties, bubble/fire/plant/snow/wither/sculk/enchantment/archetype tags, loot, recipes, Nether generation and model dispatch",
  "test-block mode, message, redstone edge latches and block-based outcome scan",
  "test-instance record, geometry, template placement/capture, GameTest handoff and result state",
  "conduit water/frame activity, player-effect radius, target reference and attack cadence",
  "beacon beam sections, pyramid level, effect powers, payment and construct criterion",
  "sign form/support, two-sided text, wax, editor lease, applicators and click actions",
  "skull/head form, power, profile/sound/name, note instrument, animation and wither-summon dispatch",
  "honeycomb's code-built unwaxed-to-waxed copper replacement map and commit effects",
  "chest pairing, obstruction, per-half storage/opener state and trapped-chest signal dispatch",
  "hopper power latch, container/sided dispatch, push/pull/rollback, loose-item absorption and cooldown",
  "dispenser power latch, reservoir selection, 80-entry identity registry and component/tag/class fallback dispatch",
  "item, inventory and progression state",
  "entity, mob and effect state",
  "classic-neutral anger targets/timers and Piglin anger memories",
  "raid omen, manager lifecycle, waves, membership and rewards",
  "rule-gated entity itemization and its pre-read state mutations",
  "generated chunk and structure state",
]
persistence = [
  "registered content persists through the state domain owned by its behavior family",
  "data-only parameters remain version-locked inputs rather than independent algorithms",
  "brushable blocks persist loot or materialized item while direction, count and timers reset",
  "sensor frequency, current/waiting vibration and travel delay survive chunk reload",
  "jigsaw connector fields survive chunk reload and resynchronize before later edits",
  "structure-block fields survive chunk reload separately from memory/disk template-manager state",
  "structure-void block state persists generically while capture deliberately omits its coordinate",
  "exact cave/void-air palettes persist while all-air live reads can project ordinary air",
  "property-free bedrock state persists generically while below-zero retrogen can deliberately replace old bedrock",
  "property-free reinforced-deepslate state persists generically while ancient-city generation and protected replacement remain separate inputs",
  "property-free tinted-glass state persists generically while recipe, loot and impermeable snapshots remain reload-selected inputs",
  "property-free plain-glass state persists generically while loot, smelting and impermeable snapshots remain reload-selected inputs",
  "property-free slime-block state persists generically while loot and recipes remain reload-selected and brewing/debug selectors remain code-built",
  "property-free honey-block state persists generically while loot, recipes and direct tag memberships remain reload-selected and physical/AI/debug selectors remain code-built",
  "property-free soul-sand state persists generically while tags, enchantment, archetype, loot, recipes and worldgen records remain reload-selected and concrete hooks stay code-built",
  "test-block mode/message/powered survive reload while triggered resets and can diverge from block state",
  "test-instance data and absolute error markers survive reload until an operation or runner replacement changes them",
  "conduits persist only an optional target UUID while activity, frame, clocks and animation reset",
  "beacons persist powers/name/lock but ignore saved Levels and rebuild beam/base state",
  "signs persist both raw/filtered sides plus wax while editor authorization resets",
  "skulls persist profile/sound/name while their client animation counter and active latch reconstruct",
  "chest halves persist inventory/loot/name/lock independently while pairing and lid state reconstruct",
  "hopper items/loot/name/lock and signed cooldown persist while facing/tick time reconstruct",
  "dispenser nine-slot items/loot/name/lock persist while its static behavior objects retain process-lifetime optional state",
  "raid globals and raider entity references reconstruct through separate saved records",
]
client_projection = [
  "content state projects through generic block, entity, inventory and world packets",
  "resource-pack presentation is observable only where CLI rules claim it",
  "brush strokes predict sound/dust while dust state and exposed archaeology item converge authoritatively",
  "sensor states, traveling/ambient particles, clicks and resonance project without block-entity data",
  "jigsaw orientation and edit fields project through block state and block-entity data",
  "structure mode and complete edit record project through state/entity data plus local UI and boundary rendering",
  "structure void has no ordinary world model and projects only through admitted structure-block debug geometry",
  "all air states have no ordinary world geometry and AIR item stacks project as empty",
  "bedrock state projects through four equal-weight world variants, a fixed base-model item and server map color",
  "reinforced-deepslate state projects through one top/side/bottom cube shared by its ordinary block item",
  "tinted-glass state projects through one tinted cube model shared by its ordinary block item while lighting remains server-owned",
  "plain-glass state projects through one force-translucent shared block/item model with same-glass face culling",
  "slime-block state projects through a shared inner/outer-cube block/item model with same-slime face culling",
  "honey-block state projects through a shared inner/outer-cube block/item model, same-honey face culling and entity events 53/54",
  "soul-sand state projects through one full-height cube_all block/item model while the dried-ghast below-tag consumer can emit a client-local ambient sound",
  "test-block mode projects through state and entity data while message/powered stay entity data",
  "test-instance data/markers project through entity data while status responses, bounds, beams and labels are client-owned",
  "conduit targets project through entity data while activity and hunting derive independently on clients",
  "beacon menu data projects live choices while clients independently derive beam sections and levels",
  "sign text/wax projects through block-entity data while editor UI and glow/outline rendering are client-owned",
  "skull state/profile/sound/name project through block/entity data while animation, skins and geometry are client-owned",
  "chest block state and count events drive single/double lid geometry while menu/inventory packets own contents",
  "hopper enabled state projects through ordinary block updates while its model ignores enabled and menus project slots",
  "dispenser triggered state projects while facing-only models ignore it and ordered level events project each behavior",
  "raid state projects through boss events, horn packets, stats, criteria and effects",
]
protocol_families = [
  "PROTO-PLAY-CLIENTBOUND-BLOCK-001",
  "PROTO-PLAY-CLIENTBOUND-PARTICLE-001",
  "PROTO-PLAY-CLIENTBOUND-SOUND-001",
  "PROTO-PLAY-SERVERBOUND-ANVIL-BEACON-001",
  "PROTO-PLAY-SERVERBOUND-SIGN-UPDATE-001",
  "PROTO-PLAY-CLIENTBOUND-SPECIAL-SCREENS-001",
  "PROTO-PLAY-CLIENTBOUND-CONTAINER-CONVERGENCE-001",
]
status = "InProgress"
evidence = ["OFF-REPORT-001", "OFF-DATA-001", "OFF-SERVER-001"]
unknowns = [
  """
Implementation, codec and data-selected dispatch roots are inventoried, but the catalog still
exposes 775 Unreviewed IDs across four fallback families, so exhaustive ownership is not justified.
""",
]
reproduction = [
  """
Follow content-dispatch-roots.md for each fallback ID; replace it with an audited exact/pattern
family or justified DataOnly classification while retaining exact one-owner catalog coverage.
""",
]
last_commit = "docs(mc-reference): specify soul sand runtime"

[[surface]]
id = "SURFACE-PLAYER-LIFECYCLE-001"
kind = "PlayerLifecycle"
boundary = "Join, spawn, dimension entry, death, respawn, sleep, mode, ability and removal lifecycle."
triggers = [
  "login completion and initial placement",
  "death, respawn, dimension transfer and bed interaction",
  "game-mode, ability, permission and disconnect changes",
]
inventory_sources = ["OfficialServerSymbols", "PacketReport", "SaveStateFields"]
selectors = [
  "all eight source-root phase families in player-lifecycle-roots.md",
  "spawn point, bed, portal and respawn configuration paths",
  "mode, ability and permission synchronization",
]
owners = ["PLY-001", "PLY-005", "BLK-SIGN-001", "BLK-SKULL-001", "ITM-CHEST-001", "ITM-HOPPER-001", "ITM-DISPENSER-001", "ENT-005", "ENT-007", "ENT-008", "WGEN-005"]
state_domains = [
  "player identity, position, mode, abilities and progression",
  "health, death state, inventory and spawn point",
  "dimension membership and connection ownership",
  "transient sign-editor authorization tied to a live in-range player UUID",
  "player identity copied by FillPlayerHead and live nearby-player audience for wither summon criteria",
  "current chest menu, spectator admission, statistic and piglin side effects tied to a live player",
  "current hopper menu and statistic tied to a live player while automation remains world-owned",
  "current dispenser menu and inspect statistic tied to a player while scheduled dispatch remains world-owned",
]
persistence = [
  "selected player state survives disconnect and process restart",
  "session-only transport, latency and acknowledgement state does not persist",
  "a copied player profile persists with the skull entity independently of the source player's session",
]
client_projection = [
  "login/play entry, respawn, position, abilities, health and inventory convergence",
  "player-info, death, game event and disconnect presentation",
  "sign editor closes locally and its server lease clears after player loss or range failure",
  "player-head naming/skin projection and summoned-entity criteria use the admitted live player/profile result",
  "player removal closes the current chest menu and removes that user from per-half recount truth",
  "player removal closes a hopper menu without stopping passive world automation",
  "player removal closes a dispenser menu without cancelling an already retained scheduled dispatch",
]
protocol_families = [
  "PROTO-PLAY-SERVERBOUND-ENTRY-001",
  "PROTO-PLAY-CLIENTBOUND-ENTRY-001",
  "PROTO-PLAY-CLIENTBOUND-PLAYER-PROJECTION-001",
  "PROTO-PLAY-CLIENTBOUND-PLAYER-INFO-REMOVE-001",
]
status = "InProgress"
evidence = ["OFF-SERVER-001", "OFF-REPORT-001"]
unknowns = [
  """
The eight lifecycle phase families and their primary source roots now have explicit owners, but
caller/branch exhaustion, the persisted-field ledger, rollback/idempotence and reconnect/restart
continuity remain open exactly as recorded in player-lifecycle-roots.md.
""",
]
reproduction = [
  """
Expand each player-lifecycle-roots.md row through all locked callers and branches, cross every
read/written field with PersistenceReload, and replay the named join/death/respawn/relocation/
disconnect/restart matrix before promoting the surface.
""",
]
last_commit = "docs(mc-reference): specify skull runtime"

[[surface]]
id = "SURFACE-WORLD-LIFECYCLE-001"
kind = "WorldLifecycle"
boundary = "World and dimension bootstrap, chunk generation/load/activity/unload, border and shutdown."
triggers = [
  "server and level bootstrap",
  "chunk request, generation status, ticket transition and unload",
  "dimension transfer, border update, save and server shutdown",
]
inventory_sources = ["OfficialServerSymbols", "RegistryReport", "SaveStateFields"]
selectors = [
  "all eight source-root families in world-lifecycle-roots.md",
  "all chunk status, ticket-type and activity-promotion transitions",
  "dimension, portal, border, save, unload and shutdown boundaries",
]
owners = ["SIM-002", "SIM-005", "WGEN-001", "WGEN-004", "WGEN-005", "WGEN-006", "BLK-JIGSAW-001", "BLK-STRUCTURE-001", "BLK-STRUCTURE-VOID-001", "BLK-AIR-001", "BLK-BEDROCK-001", "BLK-REINFORCED-DEEPSLATE-001", "BLK-SLIME-001", "BLK-HONEY-001", "BLK-SOUL-SAND-001", "BLK-TEST-INSTANCE-001", "BLK-SKULL-001", "ITM-CHEST-001", "ITM-HOPPER-001", "ITM-DISPENSER-001"]
state_domains = [
  "dimension registry and level-global state",
  "chunk generation, loaded and activity state",
  "entities, block entities, scheduled work and points of interest within chunks",
  "operator-triggered jigsaw generation and its immediate structure writes",
  "operator/redstone structure-template capture, cache/disk selection and immediate placement writes",
  "structure-void capture omission and jigsaw connector-cell skip versus raw template writes",
  "air/cave-air/void-air bounds, section-count/read shortcuts and fixed generation roles",
  "bedrock surface/flat/End generation, retrogen, portal search and feature-protection identity roles",
  "reinforced-deepslate ancient-city generation and feature-protected target identity roles",
  "slime-block diagnostic selection inside DEBUG_AQUIFERS noise-chunk fill stripes",
  "honey-block at-or-above-sea diagnostic selection inside DEBUG_AQUIFERS noise-chunk fill stripes",
  "soul-sand normal Nether surface, ore, spring, carver, fortress, fossil, basalt-column and generation-region postprocess roles",
  "test-instance forced chunks, cleared/template-written volume and in-place runner replacement",
  "skull block-entity availability, transient animation reconstruction and wither pattern mutation across chunk boundaries",
  "chest half identity/pairing and block-entity availability across chunk activity boundaries",
  "hopper ticker admission, container discovery and exact saved cooldown across chunk activity boundaries",
  "dispenser block-entity availability and scheduled-trigger admission across chunk activity boundaries",
]
persistence = [
  "world-global, chunk-local and entity state cross distinct save/unload boundaries",
  "ticket and transient activity state may be reconstructed rather than persisted",
  "structure-block entity and named template cache/disk continuity cross distinct lifecycle boundaries",
  "captured structure-void coordinates remain absent across template cache/disk continuity",
  "all-air section palettes persist exactly while live all-air reads collapse to ordinary air",
  "bedrock palettes persist normally while explicit below-zero retrogen owns old-bedrock replacement",
  "reinforced-deepslate palettes persist normally while structure placement and target protection remain generation-owned",
  "debug-aquifer slime palettes persist as ordinary states after the explicitly gated generation write",
  "debug-aquifer honey palettes persist as ordinary states after the explicitly gated generation write",
  "generated soul-sand palettes persist as ordinary state 6998 after surface, feature, carver or structure writes",
  "test-instance record/markers, forced chunks and captured templates cross distinct continuity boundaries",
  "dispenser inventory/block state and scheduled-tick records cross distinct chunk persistence paths",
  "skull durable fields cross chunk persistence while client animation reconstructs from a fresh or retained instance",
]
client_projection = [
  "configuration registries and initial dimension entry",
  "terrain, chunk unload, world border, time, weather and dimension changes",
  "structure block/entity projection unloads with its chunk while named templates remain manager-owned",
  "structure void remains ordinarily invisible while admitted structure-block scans project its marker",
  "exact mixed-section air state IDs remain invisible and out-of-bounds reads project void air",
  "bedrock state 85 projects as ordinary terrain while generation and portal consumers retain their own lifecycle",
  "reinforced-deepslate state 32085 projects as ordinary terrain while ancient-city and protection consumers retain their lifecycle",
  "debug-generated slime-block state 12532 projects as ordinary terrain after chunk publication",
  "debug-generated honey-block state 21816 projects as ordinary terrain after chunk publication",
  "generated soul-sand state 6998 projects as ordinary full-height terrain after chunk publication",
  "test-instance entity data unloads with its chunk while forced chunks and manager templates are separate",
  "skull block/entity projection unloads and reconstructs with the containing chunk",
]
protocol_families = [
  "PROTO-CONFIGURATION-CLIENTBOUND-REQUIRED-001",
  "PROTO-PLAY-CLIENTBOUND-TERRAIN-001",
  "PROTO-PLAY-CLIENTBOUND-WORLD-BORDER-001",
]
status = "InProgress"
evidence = ["OFF-SERVER-001", "OFF-REPORT-001", "EXP-WGEN-001"]
unknowns = [
  """
Bootstrap, chunk, ticket, publication, unload and shutdown roots are inventoried, but their complete
transition graph and cross-domain ordering are not yet expanded into implementation-level rules.
""",
]
reproduction = [
  """
Complete the status/ticket/publication transition graph; trace one level through bootstrap,
generation, activity, save, unload, reload and shutdown; assign every ordering edge to a leaf rule.
""",
]
last_commit = "docs(mc-reference): specify soul sand runtime"

[[surface]]
id = "SURFACE-PERSISTENCE-RELOAD-001"
kind = "PersistenceReload"
boundary = "Observable state continuity across chunk unload/reload, disconnect/rejoin and restart."
triggers = [
  "chunk save, unload and reload",
  "player save, disconnect and rejoin",
  "world save, shutdown and restart",
]
inventory_sources = ["SaveStateFields", "OfficialServerSymbols"]
selectors = [
  "all eight continuity families in persistence-reload-roots.md",
  "pending timers, queues, identities, references and random state with observable continuity",
  "defaulting, migration and reconstruction branches in the locked version",
]
owners = ["SIM-003", "BLK-007", "BLK-SCULK-SENSOR-001", "BLK-JIGSAW-001", "BLK-STRUCTURE-001", "BLK-STRUCTURE-VOID-001", "BLK-AIR-001", "BLK-TEST-BLOCK-001", "BLK-TEST-INSTANCE-001", "BLK-CONDUIT-001", "BLK-BEACON-001", "BLK-SIGN-001", "BLK-SKULL-001", "ITM-CHEST-001", "ITM-HOPPER-001", "ITM-DISPENSER-001", "ITM-001", "ENT-001", "WGEN-001"]
state_domains = [
  "world and dimension globals",
  "chunk, block-entity and scheduled state",
  "sensor frequency, selector/current vibration, delay and phase schedule continuity",
  "jigsaw name, target, pool, final state, joint and priority continuity",
  "structure name/author/metadata/geometry/settings/latch continuity plus distinct template-manager state",
  "structure-void world state versus deliberately absent captured-template coordinates",
  "exact stored air palettes versus ordinary-air all-air read projection and transient AIR item absence",
  "test-block mode, message and powered continuity plus transient triggered reset",
  "test-instance six-field data and marker continuity plus successful-run entity replacement",
  "conduit optional target-UUID continuity plus reset activity, frame, counters and clocks",
  "beacon power/name/lock continuity plus ignored saved level and reconstructed beam/base state",
  "sign front/back raw and filtered text, color, glow and wax continuity plus reset editor UUID",
  "skull nullable profile, note sound and custom name continuity plus transient animation retention/reset",
  "independent chest-half items, pending loot, seed, name and lock plus reconstructed pairing/opener/lid state",
  "hopper items, pending loot, seed, name, lock and exact signed cooldown plus reconstructed facing/tick time",
  "dispenser nine items, pending loot, seed, name and lock plus separately persisted triggered state/scheduled work",
  "entity and player state",
  "inventories, progression and data-driven runtime state",
]
persistence = [
  "this surface is the explicit owner of save/reload continuity semantics",
  "original file formats remain outside Ferrite's compatibility requirement",
  "sensor listener records may use another encoding only if reload resumes the same winner and delay",
  "jigsaw reload must preserve valid fields and reproduce every missing/invalid default",
  "structure reload clamps offsets/sizes, defaults invalid mode to data and keeps entity versus template durability distinct",
  "structure-void state reloads generically while prior capture omission reconstructs no template cell",
  "air palette identities reload exactly, while a zero-count section remains live-readable as ordinary air",
  "test-block reload defaults invalid mode to fail without rewriting its separately loaded block state",
  "test-instance reload retains the constructed record on whole-data decode failure and rebuilds markers independently",
  "conduit target changes project without independently dirtying, and missing/malformed Target loads null",
  "beacon selection dirties without immediate update; load filters powers and ignores serialized Levels",
  "sign load defaults malformed sides, resolves components at the sign position and never restores editor authorization",
  "skull load independently nulls missing/malformed fields and leaves existing transient animation untouched",
  "chest load chooses pending loot versus items per half; legacy upgrade may pair and swap list ownership",
  "hopper load chooses pending loot versus five items and defaults absent TransferCooldown to -1",
  "dispenser load chooses pending loot versus nine items while process-global behavior success is not world-save state",
]
client_projection = [
  "a reloaded state must converge to the same client-observable result as uninterrupted state",
  "transient interpolation and transport state may restart where explicitly documented",
  "sensor reload reprojects its in-flight particle without exposing private block-entity state",
  "jigsaw reload reprojects editable fields through its ordinary block-entity update tag",
  "structure reload reprojects its complete custom record and flags-2 mode reconciliation",
  "structure-void reload remains invisible until an admitted structure-block scan projects it",
  "reloaded air variants remain invisible and AIR cannot reload as a positive inventory stack",
  "test-block reload reprojects mode/message/powered while triggered remains server-local and reset",
  "test-instance reload reprojects its complete data/marker tag before later action or runner convergence",
  "conduit reload reprojects Target before the first independently derived client/server frame refresh",
  "beacon reload retains powers but waits for scan publication and a later 80-tick level refresh",
  "sign reload reprojects both sides and wax through its ordinary update tag",
  "skull reload reprojects profile/sound/name while a reconstructed client's animation begins at zero",
  "chest reload reprojects pair state and contents while opener counts/lid interpolation restart transiently",
  "hopper reload resumes its saved cooldown while the first admitted tick replaces transient tick time",
  "dispenser reload resumes inventory/block/scheduled state while static brush/TNT optional success survives only process-continuous reload",
]
protocol_families = []
status = "InProgress"
evidence = ["OFF-SERVER-001"]
unknowns = [
  """
The eight persistence/reload continuity families and their primary source roots now have owners,
but their exhaustive behavior-field matrix, write/failure ordering, default/migration branches,
reference reconstruction and first post-boundary results remain open in persistence-reload-roots.md.
""",
]
reproduction = [
  """
Expand persistence-reload-roots.md into a field-level matrix, then replay chunk reload, player
rejoin and full restart with success/missing/malformed/write-failure inputs and compare each state
plus its first subsequent transition with an uninterrupted control.
""",
]
last_commit = "docs(mc-reference): specify air identity runtime"

[[surface]]
id = "SURFACE-CLIENT-PROJECTION-001"
kind = "ClientProjection"
boundary = "Observable projection of authoritative state into an unmodified 26.2 client."
triggers = [
  "clientbound state snapshot or delta",
  "prediction rejection and correction",
  "sound, particle, level-event, UI, chat and debug presentation",
]
inventory_sources = ["PacketReport", "OfficialClientSymbols", "OfficialServerSymbols"]
selectors = [
  "all locked clientbound packet identities",
  "client listener dispatch and prediction reconciliation roots",
  "observable audiovisual and UI effects claimed by CLI-001 through CLI-006",
]
owners = [
  "CLI-001",
  "CLI-002",
  "CLI-003",
  "CLI-004",
  "CLI-005",
  "CLI-006",
  "CLI-COMMAND-FEEDBACK-001",
  "BLK-SCULK-SENSOR-001",
  "BLK-JIGSAW-001",
  "BLK-STRUCTURE-001",
  "BLK-STRUCTURE-VOID-001",
  "BLK-AIR-001",
  "BLK-BEDROCK-001",
  "BLK-REINFORCED-DEEPSLATE-001",
  "BLK-TINTED-GLASS-001",
  "BLK-GLASS-001",
  "BLK-SLIME-001",
  "BLK-HONEY-001",
  "BLK-SOUL-SAND-001",
  "BLK-TEST-BLOCK-001",
  "BLK-TEST-INSTANCE-001",
  "BLK-CONDUIT-001",
  "BLK-BEACON-001",
  "BLK-SIGN-001",
  "BLK-SKULL-001",
  "ITM-CHEST-001",
  "ITM-HOPPER-001",
  "ITM-DISPENSER-001",
]
state_domains = [
  "authoritative server state selected for a connection",
  "client prediction and acknowledgement state",
  "client-only presentation state within the documented observable boundary",
  "sensor block states and transient vibration, sculk and resonance effects",
  "jigsaw block orientation, synchronized edit record and client-local screen state",
  "structure mode models, synchronized record, local screen and permission-gated boundary geometry",
  "structure-void invisible world state, generated item model and pale-red structure-block marker",
  "air/cave-air/void-air invisible state IDs and zero-count AIR item projection",
  "bedrock state 85, four default-weight world variants, fixed item model and filled-map stone color",
  "reinforced-deepslate state 32085, one top/side/bottom cube model, shared item model and deepslate map color",
  "tinted-glass state 27161, one tinted cube block/item model and server-selected gray map color",
  "plain-glass state 562, one force-translucent block/item model and same-glass face-culling context",
  "slime-block state 12532, one inner/outer-cube block/item model and same-slime face-culling context",
  "honey-block state 21816, one inner/outer-cube block/item model, same-honey face-culling context and events 53/54",
  "soul-sand state 6998, one full-height cube_all block/item model and dried-ghast local below-tag sound context",
  "test-block four-mode state, synchronized edit record and client-local screen state",
  "test-instance synchronized data/markers, local screen/status state, beam, bounds and error labels",
  "conduit target data, client-derived activity/hunting, particles and block/item render state",
  "beacon menu data, client-derived beam sections and distance/scoping-scaled beam rendering",
  "sign editor preview/closure, both synchronized text sides and ordinary/hanging glowing-text rendering",
  "skull synchronized durable fields, client-only power animation, profile skin cache and block/item special rendering",
  "chest menu/content convergence, count-event lid interpolation, compound light and date-selected materials",
  "hopper enabled block state, facing-only baked model and five-slot menu convergence",
  "dispenser triggered block state, facing-only baked model, generic_3x3 menu and ordered level-event effects",
]
persistence = [
  "server-authoritative source state persists through its gameplay owner",
  "client interpolation, rendering and most presentation state are transient",
]
client_projection = [
  "protocol/completion.toml owns every clientbound packet family",
  "CLI rules own prediction, correction, timing and visible presentation semantics",
  "CLI-COMMAND-FEEDBACK-001 owns direct and operator command-result presentation",
  "BLK-JIGSAW-001 owns local edit UI and block-state/block-entity-data convergence",
  "BLK-STRUCTURE-001 owns local edit UI, complete convergence and boundary/invisible-block rendering",
  "BLK-STRUCTURE-VOID-001 owns ordinary invisibility and the structure-block renderer's pale-red marker input",
  "BLK-AIR-001 owns invisible air-state projection and AIR optional-stack normalization",
  "BLK-BEDROCK-001 owns full-cube block/item models and the server-selected map-color fallback",
  "BLK-REINFORCED-DEEPSLATE-001 owns the single full-cube block/item model and server-selected deepslate map color",
  "BLK-TINTED-GLASS-001 owns the shared tinted block/item model and server-selected gray map color",
  "BLK-GLASS-001 owns the force-translucent shared block/item model and same-glass face omission",
  "BLK-SLIME-001 owns the shared inner/outer-cube block/item model and same-slime face omission",
  "BLK-HONEY-001 owns the shared inner/outer-cube block/item model, same-honey face omission and five/ten-particle entity events",
  "BLK-SOUL-SAND-001 owns the full-height cube_all block/item model and the dried-ghast client's unwaterlogged 1/40 below-tag ambient sound",
  "BLK-TEST-BLOCK-001 owns local mode/message UI and state/entity convergence",
  "BLK-TEST-INSTANCE-001 owns local edit/status UI, complete convergence and beam/bounds/marker rendering",
  "BLK-CONDUIT-001 owns target-only synchronization plus independent particles and shell/cage/wind/eye projection",
  "BLK-BEACON-001 owns menu-data choices, local beam reconstruction and the 2048-high final render section",
  "BLK-SIGN-001 owns editor UI, block-entity convergence and ordinary/hanging text color/glow/outline projection",
  "BLK-SKULL-001 owns durable-field convergence, client animation, skin resolution and world/item head rendering",
  "ITM-CHEST-001 owns generic 3/6-row presentation plus regular/trapped/Christmas single/double lid rendering",
  "ITM-HOPPER-001 owns enabled-state publication, visually identical enabled/disabled models and menu slots",
  "ITM-DISPENSER-001 owns visually identical triggered states, menu slots and default/fail/projectile/animation events",
]
protocol_families = [
  "PROTO-PLAY-CLIENTBOUND-TERRAIN-001",
  "PROTO-PLAY-CLIENTBOUND-BLOCK-001",
  "PROTO-PLAY-CLIENTBOUND-ENTITY-STATE-001",
  "PROTO-PLAY-CLIENTBOUND-CONTAINER-CONVERGENCE-001",
  "PROTO-PLAY-CLIENTBOUND-PLAYER-PROJECTION-001",
  "PROTO-PLAY-CLIENTBOUND-WORLD-EFFECT-001",
  "PROTO-PLAY-CLIENTBOUND-PARTICLE-001",
  "PROTO-PLAY-CLIENTBOUND-SOUND-001",
  "PROTO-PLAY-SERVERBOUND-SIGN-UPDATE-001",
  "PROTO-PLAY-SERVERBOUND-OPERATOR-BLOCKS-001",
  "PROTO-PLAY-CLIENTBOUND-SPECIAL-SCREENS-001",
]
status = "Mapped"
evidence = ["OFF-CLIENT-001", "OFF-SERVER-001", "OFF-REPORT-001", "EXP-CLI-001"]
unknowns = []
reproduction = [
  "Run the CLI experiment matrix and protocol vectors after any projection or reconciliation change.",
]
last_commit = "docs(mc-reference): specify soul sand runtime"

[[surface]]
id = "SURFACE-DATA-RELOAD-001"
kind = "DataReload"
boundary = "Bootstrap and reload of registries, tags, recipes, loot, functions and feature flags."
triggers = [
  "server bootstrap and world registry load",
  "data-pack selection and reload",
  "reconfiguration and live-tag synchronization",
]
inventory_sources = ["RegistryReport", "BundledData", "OfficialServerSymbols", "PacketReport"]
selectors = [
  "all eight source-root families in data-reload-roots.md",
  "all gameplay registry and reload-listener dependency edges",
  "reload failure, atomic publication and active-session convergence",
]
owners = ["ITM-004", "ITM-006", "ITM-007", "ITM-CHEST-001", "ITM-HOPPER-001", "ITM-DISPENSER-001", "BLK-SCULK-SENSOR-001", "BLK-JIGSAW-001", "BLK-STRUCTURE-001", "BLK-STRUCTURE-VOID-001", "BLK-AIR-001", "BLK-BEDROCK-001", "BLK-REINFORCED-DEEPSLATE-001", "BLK-TINTED-GLASS-001", "BLK-GLASS-001", "BLK-SLIME-001", "BLK-HONEY-001", "BLK-SOUL-SAND-001", "BLK-TEST-INSTANCE-001", "BLK-BEACON-001", "BLK-SIGN-001", "BLK-SKULL-001", "WGEN-002", "WGEN-003"]
state_domains = [
  "registry and holder identity",
  "tag, recipe, loot, function and feature-flag snapshots",
  "sensor vibration, careful-step, damping, occlusion and resonator tag snapshots",
  "jigsaw stored template-pool keys and generation-time registry resolution",
  "structure-block named template lookup against the active structure-template manager",
  "replaceable tag membership and template/processor data that can select structure-void semantics",
  "air and replaceable tag membership for all three air states plus ordinary-air parrot support membership",
  "bedrock boss, wind-charge, feature-protection, geode-invalid and End-infiniburn tag memberships",
  "reinforced-deepslate boss and feature-protection memberships plus wind-charge resistance nonmembership",
  "tinted-glass impermeable membership plus the code-locked beehive caller that currently makes it non-interacting",
  "plain-glass impermeable and smelts-to-glass memberships plus their beehive non-interaction and smelting-input consumers",
  "slime-block loot plus shaped block and shapeless ball recipe snapshots; start-mix brewing edges stay code-built",
  "honey-block loot, shaped block/shapeless bottle recipes and snow-support/bounce tag snapshots; physical, piston, AI and debug selectors stay code-built",
  "soul-sand tags, Soul Speed, sulfur archetype, loot, recipes and Nether worldgen snapshots; registration, shapes, postprocess and concrete structure/feature hooks stay code-built",
  "configured test-instance holder identity and its structure/rotation/padding/required fields",
  "beacon base/payment tag snapshots at layer scan, slot admission and submit-time residue",
  "sign and hanging-sign tag snapshots used by hanging attachment, support and chain orientation",
  "wither_summon_base_blocks membership read by skull full/base pattern admission",
  "chest pending-loot keys resolve against the active loot-table snapshot while code-owned pairing stays fixed",
  "hopper pending loot and does_not_block_hoppers/beehives tag snapshots affect materialization and loose-item admission",
  "dispenser pending loot, sulfur archetype, beehive, mud, rail and other called-action tag snapshots affect later dispatch",
  "active world and connection views of reloaded data",
]
persistence = [
  "selected packs and world registry state participate in world reload",
  "derived reload snapshots are reconstructed from locked inputs",
]
client_projection = [
  "configuration registry and tag synchronization",
  "play-state reconfiguration and live-tag updates",
]
protocol_families = [
  "PROTO-CONFIGURATION-CLIENTBOUND-REQUIRED-001",
  "PROTO-PLAY-SERVERBOUND-RECONFIGURATION-001",
  "PROTO-PLAY-CLIENTBOUND-RECONFIGURATION-001",
  "PROTO-PLAY-CLIENTBOUND-LIVE-TAGS-001",
]
status = "InProgress"
evidence = ["OFF-DATA-001", "OFF-REPORT-001", "OFF-SERVER-001"]
unknowns = [
  """
Pack, registry, listener, content and session roots are inventoried, but the exact listener DAG,
failure atomicity, live-world replacement and feature-flag effects are not exhaustively specified.
""",
]
reproduction = [
  """
Complete the listener/dependency and publication-field ledgers; inject failures at every prepare,
apply and refresh boundary; test successful and failed reloads before and during active sessions.
""",
]
last_commit = "docs(mc-reference): specify soul sand runtime"

[[surface]]
id = "SURFACE-CROSS-SYSTEM-ORDERING-001"
kind = "CrossSystemOrdering"
boundary = "Observable ordering, atomicity and conflict resolution where behavior domains meet."
triggers = [
  "one event mutating state owned by two or more domains",
  "same-tick work from network, scheduled, entity, block and world phases",
  "concurrent player actions or lifecycle changes targeting shared state",
]
inventory_sources = ["ManualCrossProduct", "OfficialServerSymbols", "PacketReport"]
selectors = [
  "all 36 unordered non-meta surface pairs in cross-system-joins.toml",
  "phase boundaries, queues, callbacks, acknowledgements and save barriers",
  "same-target conflicts and multi-object transactions",
]
owners = [
  "SIM-002",
  "BLK-003",
  "ENV-004",
  "RED-006",
  "PLY-005",
  "ITM-002",
  "ENT-005",
  "MOB-001",
  "WGEN-005",
  "CLI-006",
  "BLK-SCULK-SENSOR-001",
  "BLK-JIGSAW-001",
  "BLK-STRUCTURE-001",
  "BLK-STRUCTURE-VOID-001",
  "BLK-AIR-001",
  "BLK-BEDROCK-001",
  "BLK-REINFORCED-DEEPSLATE-001",
  "BLK-TINTED-GLASS-001",
  "BLK-GLASS-001",
  "BLK-SLIME-001",
  "BLK-HONEY-001",
  "BLK-SOUL-SAND-001",
  "BLK-TEST-BLOCK-001",
  "BLK-TEST-INSTANCE-001",
  "BLK-CONDUIT-001",
  "BLK-BEACON-001",
  "BLK-SIGN-001",
  "BLK-SKULL-001",
  "ITM-HONEYCOMB-001",
  "ITM-CHEST-001",
  "ITM-HOPPER-001",
  "ITM-DISPENSER-001",
]
state_domains = [
  "all authoritative gameplay domains shared by two or more root surfaces",
  "transport acknowledgement and client projection at commit boundaries",
  "persistence barriers for multi-domain mutations",
  "dispenser scheduled dispatch joining inventory, block, entity, item, game-rule, event and client-effect domains",
  "structure operator/redstone actions joining packet order, block/entity state, template cache/disk and world writes",
  "structure-void replacement, fluid rejection, capture omission, processor skip and debug projection joins",
  "air state identity joining bounds, chunk counters, palette continuity, generation and client projection plus AIR stack ingress/egress",
  "bedrock identity joining break/piston/explosion gates, entity and fire consumers, test-create and generation/protection, tags and projection",
  "reinforced-deepslate identity joining break/loot, piston/explosion gates, boss and feature tags, ancient-city generation, creative inventory and projection",
  "tinted-glass identity joining break/loot, lighting/beacon, entity/golem support, impermeable reload, recipe and projection",
  "plain-glass identity joining break/loot, lighting/beacon, entity/golem support, recipe/tag reload and face-culling projection",
  "slime-block identity joining break/loot, collision/fall/step velocity, piston adhesion/entity motion, recipe/brewing, debug generation and projection",
  "honey-block identity joining break/loot, fall/side-slide movement and effects, piston adhesion/surface carrying, tag/AI/recipe reload, debug generation and projection",
  "soul-sand identity joining break/loot, reduced collision and movement, scheduled bubble updates, fire/plant/snow/wither/sculk/Soul Speed/archetype reload, normal Nether generation and projection",
  "test-instance actions joining packet order, data/markers, forced chunks, template writes and GameTest callbacks",
  "skull placement joining power, persisted components, note sound, profile/skin projection and destructive wither admission",
]
persistence = [
  "multi-domain state must not expose a reload result forbidden by its live atomicity contract",
  "pending cross-domain work requires an explicit persist, cancel or reconstruct rule",
]
client_projection = [
  "observers must see a permitted commit order rather than an impossible intermediate state",
  "corrections and effects follow the owning transaction's acceptance boundary",
]
protocol_families = [
  "PROTO-PLAY-SERVERBOUND-MOVEMENT-001",
  "PROTO-PLAY-SERVERBOUND-BLOCK-001",
  "PROTO-PLAY-SERVERBOUND-CONTAINER-CONVERGENCE-001",
  "PROTO-PLAY-SERVERBOUND-ADMIN-STATE-001",
  "PROTO-PLAY-SERVERBOUND-SIGN-UPDATE-001",
  "PROTO-PLAY-CLIENTBOUND-BLOCK-001",
  "PROTO-PLAY-CLIENTBOUND-ENTITY-STATE-001",
  "PROTO-PLAY-CLIENTBOUND-CONTAINER-CONVERGENCE-001",
]
status = "InProgress"
evidence = ["OFF-SERVER-001", "OFF-REPORT-001", "EXP-SIM-001"]
unknowns = [
  """
The complete unordered surface cross-product is recoverably partitioned, but every join still needs
its exact admission, ordering, atomicity, failure, persistence and projection rules and vectors.
""",
]
reproduction = [
  """
Resolve each InProgress row in cross-system-joins.toml to owning rules and conflict/order vectors;
the validator rejects missing, duplicate, ownerless or falsely Mapped joins.
""",
]
last_commit = "docs(mc-reference): specify soul sand runtime"
