# Experiment results

No directed experiment has yet been promoted to `observed` in this first breadth pass. Every
unresolved high-impact branch has a complete `planned` definition in
[`definitions.toml`](definitions.toml); until a result is added, the linked leaf rule retains
`Cross-checked` or `Implementation blocker` status.

A result entry must contain: experiment ID; 26.2 metadata, client/server SHA-1; Java/runtime and
server/client configuration; exact definition revision; repetition count and RNG seed where
controlled; observed tick-indexed sequence; deviations; and the rule/evidence status change. Never
paste logs, generated reports, decompiled text or world data.
